<!doctype html>
<html lang="id">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#23634c">
<meta name="description" content="Harvest Harbor — tumbuhkan kebun kecil menjadi kota pesisir dengan pertanian, crafting, riset, dan perdagangan laut.">
<title>Harvest Harbor — Kebunmu, cakrawalamu</title>
<style>
:root{--ink:#253d35;--muted:#73847b;--green:#26634d;--light:#edf3e8;--cream:#fbfaf5;--line:#e4e8dd;--gold:#ebbb51;--danger:#a34636;--shadow:0 12px 40px #273a3110;font-family:Inter,"Segoe UI",Arial,"Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji",sans-serif;color:var(--ink);background:var(--cream);font-size:14px}*{box-sizing:border-box}body{margin:0}button,input,select{font:inherit}button{cursor:pointer;border:0;background:var(--green);color:white;padding:11px 15px;border-radius:10px;font-weight:650;transition:background .15s,transform .15s}button:hover{filter:brightness(1.06)}button:active{transform:translateY(1px)}button:disabled{opacity:.48;cursor:not-allowed;transform:none}button:focus-visible,input:focus-visible,select:focus-visible,canvas:focus-visible{outline:3px solid #d1a440;outline-offset:3px}button.secondary{background:#eaf0e5;color:var(--green)}button.ghost{background:transparent;color:var(--muted);border:1px solid var(--line)}button.danger{background:#fae9e4;color:var(--danger)}button.small{padding:7px 10px;font-size:12px}button.icon{padding:8px 11px}h1,h2,h3,h4,p{margin-top:0}h1,h2,h3{letter-spacing:-.035em}h2{font-size:25px;margin-bottom:7px}h3{font-size:17px;margin-bottom:8px}h4{font-size:14px;margin-bottom:6px}p{line-height:1.55}small,.muted{color:var(--muted)}small{font-size:12px;line-height:1.5}a{color:var(--green)}input,select{width:100%;background:white;border:1px solid #d7dfd4;border-radius:9px;padding:11px;color:var(--ink);min-width:0}label{display:block;font-size:12px;font-weight:600;margin:12px 0 5px}hr{border:0;border-top:1px solid var(--line);margin:20px 0}.hidden{display:none!important}.row{display:flex;align-items:center;gap:9px}.between{justify-content:space-between}.wrap{flex-wrap:wrap}.grow{flex:1;min-width:0}.grid2{display:grid;grid-template-columns:1fr 1fr;gap:10px}.stack{display:grid;gap:12px}.pill{padding:5px 8px;font-size:10px;letter-spacing:.05em;font-weight:700;border-radius:6px;background:var(--light);color:var(--green);white-space:nowrap}.pill.gold{background:#fcf1cf;color:#926815}.pill.red{background:#fbe8e2;color:var(--danger)}.eyebrow{font-size:10px;font-weight:800;letter-spacing:.17em;color:var(--muted);text-transform:uppercase}.titlefont{font-family:"Trebuchet MS","Segoe UI",sans-serif}.card{background:white;border:1px solid var(--line);border-radius:15px;padding:16px;box-shadow:0 3px 8px #233b301}details.card{margin-bottom:12px}details summary{cursor:pointer;font-weight:700}.notice{border:1px solid #efd999;background:#fffae8;border-radius:11px;padding:13px;color:#796321;font-size:13px;line-height:1.5}.error{border-color:#edbdb3;background:#fff0eb;color:#993e2d}.notice:empty{display:none}.progress{height:6px;background:#eef1e8;border-radius:6px;overflow:hidden;margin:10px 0}.progress>i{display:block;height:100%;background:#7faa70;border-radius:6px;transition:width .6s linear}.material{font-size:11px;line-height:1.8;color:var(--muted)}.material .missing{color:var(--danger)}.bigicon{width:45px;height:45px;background:var(--light);border-radius:12px;display:grid;place-items:center;font-size:25px;flex-shrink:0}.price{font-weight:750;color:#9b7224}.sectiongap{margin-top:24px}.empty{padding:22px 14px;text-align:center;border:1px dashed #cdd7c7;border-radius:12px;color:var(--muted);line-height:1.7}.mini{font-size:11px}.full{width:100%}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:12px}.spacer{height:12px}
#gate{min-height:100dvh;display:grid;grid-template-columns:1.05fr 1fr;overflow:hidden}.gate-art{position:relative;background:#dcebbd;display:flex;flex-direction:column;justify-content:space-between;padding:55px;overflow:hidden}.brand{display:flex;align-items:center;gap:10px}.brand-mark{width:43px;height:43px;display:grid;place-items:center;background:var(--green);border-radius:13px;color:white;font-size:25px;box-shadow:inset 0 -3px #0002}.brand strong{font-size:20px;letter-spacing:-.05em}.brand small{display:block;font-size:9px;letter-spacing:.2em;margin-top:4px}.gate-art h1{font-size:clamp(42px,5.5vw,75px);line-height:1.04;position:relative;z-index:2;max-width:570px;margin:65px 0 20px}.gate-art p{max-width:350px;position:relative;z-index:2}.gate-land{position:relative;height:300px;flex:1;min-height:230px}.gate-land:before{content:"";position:absolute;right:-100px;bottom:-160px;border-radius:50%;background:#79bdc5;width:600px;height:380px;transform:rotate(-23deg);border:22px solid #e8d9a3}.gate-land .hill{position:absolute;bottom:40px;left:-40px;width:320px;height:180px;border-radius:50%;background:#98bb72;transform:rotate(-12deg)}.art-house{position:absolute;bottom:70px;left:30px;font-size:130px;filter:drop-shadow(0 10px 0 #65834b22)}.art-boat{position:absolute;bottom:-20px;right:15px;font-size:95px;animation:bob 4s ease-in-out infinite}.art-tree{position:absolute;font-size:90px;left:190px;bottom:80px}.art-crop{position:absolute;font-size:48px;left:-5px;bottom:20px;letter-spacing:4px;transform:rotate(-8deg)}.gate-form{display:flex;align-items:center;justify-content:center;padding:40px;background:var(--cream)}.gate-box{max-width:400px;width:100%}.gate-box h2{font-size:32px;margin-top:12px}.gate-box form{margin-top:23px}.gate-tabs{display:flex;gap:6px;background:#eef1e8;padding:5px;border-radius:11px;margin:20px 0}.gate-tabs button{flex:1;background:transparent;color:var(--muted)}.gate-tabs button.active{background:white;color:var(--green);box-shadow:0 2px 8px #233b300a}.gate-links{display:flex;justify-content:space-between;margin-top:15px;gap:8px}.gate-links button{padding:0;color:var(--green);background:none;font-size:12px}.spinner{width:30px;height:30px;border:3px solid #d8e4cd;border-top-color:var(--green);border-radius:50%;animation:spin 1s linear infinite;margin:30px auto}.checkline{font-size:11px;color:var(--muted);margin:15px 0}.footer-note{font-size:11px;position:relative;margin-top:25px;color:#526749}
#app{height:100dvh;display:grid;grid-template-columns:86px minmax(0,1fr);grid-template-rows:78px minmax(0,1fr)}.topbar{grid-column:1/-1;background:#fffefa;border-bottom:1px solid var(--line);padding:0 26px;display:flex;gap:28px;align-items:center;z-index:5}.topbar .brand{min-width:200px}.hud{display:flex;gap:22px;align-items:center;flex:1;overflow:auto;scrollbar-width:none}.huditem{display:flex;gap:8px;align-items:center;white-space:nowrap}.huditem>span{font-size:23px}.huditem strong{font-size:16px;display:block;letter-spacing:-.03em}.huditem small{font-size:9px;text-transform:uppercase;letter-spacing:.08em}.savecontrol{display:flex;align-items:center;gap:13px;white-space:nowrap}.statusdot{display:inline-block;width:6px;height:6px;border-radius:50%;background:#74a670;margin-right:6px}.statusdot.dirty{background:#d9a53f}.statusdot.offline{background:#c45d45}.sidebar{background:#fffefa;border-right:1px solid var(--line);padding:20px 8px 12px;display:flex;flex-direction:column;gap:6px;overflow:auto;scrollbar-width:none}.navbtn{background:transparent;color:#7e8b7b;font-weight:500;font-size:10px;border-radius:12px;padding:12px 2px;display:flex;flex-direction:column;align-items:center;gap:7px;flex-shrink:0}.navbtn .navicon{font-size:22px;filter:grayscale(.35)}.navbtn.active{background:#e7efe0;color:#285d44;font-weight:700}.navbtn.active .navicon{filter:none}.workspace{display:grid;grid-template-columns:minmax(0,1fr) 374px;min-height:0}.map-section{display:flex;flex-direction:column;min-width:0;min-height:0;padding:26px 23px 18px;background:#f3f5ea}.map-heading{display:flex;align-items:center;justify-content:space-between;margin-bottom:17px;gap:12px}.map-heading h1{font-size:27px;margin:7px 0 0}.weather{background:#fff9dd;padding:9px 11px;border-radius:10px;white-space:nowrap;font-size:11px}.map-wrap{flex:1;position:relative;min-height:250px;border-radius:20px;overflow:hidden;border:1px solid #bed09f;box-shadow:var(--shadow);background:#a9c887}#world{width:100%;height:100%;display:block;touch-action:none;cursor:grab}.map-overlay{position:absolute;left:15px;top:15px;display:flex;gap:7px;pointer-events:none}.map-badge{background:#fffdf2eb;backdrop-filter:blur(6px);padding:7px 10px;border-radius:7px;font-size:10px;letter-spacing:.06em;color:#496540;font-weight:650;box-shadow:0 2px 4px #0001}.map-controls{position:absolute;bottom:15px;right:15px;display:flex;gap:4px;align-items:center}.map-controls button{background:#fffdf4;color:var(--green);padding:8px 12px;box-shadow:0 3px 8px #0001}.map-tip{position:absolute;bottom:16px;left:16px;max-width:65%;color:#344c34;font-size:10px;background:#fffdf0e0;border-radius:7px;padding:8px 10px;pointer-events:none}#placementHint{position:absolute;top:52px;left:15px;right:15px;display:flex;align-items:center;justify-content:space-between;gap:8px;background:#fffdf7ed;border:1px solid #dfe8d5;border-radius:10px;padding:10px 12px;font-size:12px;box-shadow:var(--shadow)}.goal-card{display:flex;gap:13px;align-items:center;margin-top:16px;padding:14px 16px;border-radius:13px;background:#fffefa;border:1px solid var(--line)}.goal-icon{font-size:23px}.goal-card strong{font-size:12px;display:block;margin:3px 0 4px}.goal-card small{font-size:11px;line-height:1.35;display:block}.right-panel{background:var(--cream);border-left:1px solid var(--line);display:flex;flex-direction:column;min-width:0;min-height:0}.panel-header{padding:26px 23px 18px;border-bottom:1px solid var(--line)}.panel-header h2{margin:7px 0 0;font-size:24px}.panel-scroll{padding:20px 21px 28px;overflow:auto;flex:1;scrollbar-width:thin;scrollbar-color:#c4d0bc transparent}.panel-intro{font-size:12px;color:var(--muted);margin-bottom:17px}.crop-card{color:var(--ink);display:flex;align-items:center;gap:11px;padding:12px;border:1px solid var(--line);border-radius:12px;background:white;margin-bottom:9px;cursor:pointer}.crop-card.selected{border-color:#759b69;background:#f1f6e9;box-shadow:0 0 0 1px #759b6920}.crop-card .bigicon{background:#f8f4df;width:43px;height:43px}.crop-card strong{font-size:13px}.crop-card small{display:block;font-size:10px;margin-top:4px}.crop-card .radio{width:15px;height:15px;border-radius:50%;border:1px solid #c6d2bb}.crop-card.selected .radio{border:4px solid #3f7658;background:white}.build-card{margin-bottom:12px}.build-card p{font-size:12px;line-height:1.5;margin:10px 0;color:var(--muted)}.build-card .bigicon{background:#f2f1e6}.build-card button{font-size:12px;padding:9px 12px}.build-card.locked{background:#f7f7f1}.build-card.locked .bigicon{filter:grayscale(.75)}.inventory-row{display:grid;grid-template-columns:30px 1fr auto;gap:9px;align-items:center;padding:11px 0;border-bottom:1px solid var(--line);font-size:12px}.inventory-row>span{font-size:23px}.inventory-row strong{font-size:14px}.inventory-row small{display:block;font-size:10px;margin-top:4px}.worker-row{display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px solid var(--line);font-size:12px}.worker-row .row{gap:9px}.worker-row button{padding:3px 9px;border-radius:6px}.statgrid{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin:15px 0}.statbox{padding:13px;background:#eef2e7;border-radius:11px}.statbox small{font-size:10px}.statbox strong{display:block;font-size:21px;margin-top:5px;letter-spacing:-.04em}.plotlist{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.plotbtn{padding:10px 4px;background:#ece7d5;color:var(--ink);font-weight:500;min-height:60px;border:1px solid #ded7c1;font-size:21px}.plotbtn small{display:block;font-size:9px;margin-top:6px}.plotbtn.ripe{background:#e3edcd;border-color:#8cac65}.recipe{border:1px solid var(--line);border-radius:12px;padding:13px;margin:10px 0;background:white}.recipe strong{font-size:13px}.recipe .actions button{font-size:11px;padding:7px 10px}.queue-item{display:flex;justify-content:space-between;font-size:12px;padding:8px 0;border-bottom:1px solid var(--line)}.techbranch{font-size:10px;color:#9a8252;letter-spacing:.1em;text-transform:uppercase;margin:6px 0}.route-card{border-left:3px solid #76aaae}.ship-card{background:#f2f6f4;border-color:#cedfda}.contract{border:1px solid #e7dfc7;background:#fffdf5;border-radius:11px;padding:12px;margin-bottom:9px}.contract strong{font-size:13px}.log-row{display:flex;justify-content:space-between;gap:8px;padding:8px 0;border-bottom:1px solid var(--line);font-size:11px}.log-row span{max-width:75%}.achievement{display:flex;gap:10px;align-items:center;font-size:12px;padding:8px 0}.achievement.off{opacity:.4}.filterrow{display:flex;gap:5px;overflow:auto;padding-bottom:12px}.filterrow button{font-size:10px;padding:7px 10px;white-space:nowrap}.toastarea{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);z-index:100;display:grid;gap:8px;pointer-events:none;width:min(90vw,500px)}.toast{background:#234d3dee;color:white;padding:13px 17px;border-radius:12px;font-size:13px;box-shadow:0 8px 30px #0002;line-height:1.4}.toast.bad{background:#913f30ee}.modal-backdrop{position:fixed;inset:0;background:#20382f88;backdrop-filter:blur(4px);display:grid;place-items:center;z-index:80;padding:20px}.modal{background:var(--cream);padding:26px;border-radius:20px;max-width:480px;width:100%;max-height:90dvh;overflow:auto;box-shadow:0 20px 100px #0003}.modal p{font-size:13px;color:var(--muted);white-space:pre-line}.modal .actions{justify-content:flex-end}.modal input{margin:10px 0}.online-note{font-size:10px;line-height:1.5;color:var(--muted);margin-top:12px}.sound-toggle{accent-color:var(--green);width:18px;height:18px}.loading-cover{position:absolute;inset:0;display:grid;place-items:center;background:#fbfaf5cc;z-index:9}.mini-sub{font-size:10px;color:var(--muted);line-height:1.5}
@keyframes spin{to{transform:rotate(360deg)}}@keyframes bob{50%{transform:translateY(-8px) rotate(3deg)}}
@media(min-width:1600px){.workspace{grid-template-columns:minmax(0,1fr) 410px}.map-section{padding:30px}.panel-scroll{padding:24px 26px}.topbar{padding:0 30px}.hud{gap:38px}}
@media(max-width:1180px){.topbar{gap:18px;padding:0 18px}.topbar .brand{min-width:150px}.topbar .brand strong{font-size:17px}.hud{gap:14px}.huditem strong{font-size:14px}.huditem>span{font-size:20px}.savecontrol #saveStatus{display:none}.workspace{grid-template-columns:minmax(0,1fr) 335px}.map-section{padding:20px 16px}.panel-scroll{padding:17px}.weather{display:none}}
@media(max-width:820px){#app{grid-template-columns:65px 1fr;grid-template-rows:68px minmax(0,1fr)}.topbar{padding:0 12px;gap:12px}.topbar .brand{min-width:0}.topbar .brand div:last-child{display:none}.brand-mark{width:36px;height:36px;font-size:22px}.hud{gap:16px}.huditem>span{font-size:17px}.huditem strong{font-size:12px}.savecontrol{gap:0}.savecontrol button{font-size:10px;padding:9px}.workspace{grid-template-columns:minmax(0,1fr) 300px}.sidebar{padding:14px 5px}.navbtn{font-size:9px;padding:11px 1px}.navbtn .navicon{font-size:20px}.map-heading h1{font-size:21px}.map-section{padding:18px 12px}.map-heading .eyebrow{font-size:8px}.goal-card{padding:11px}.goal-icon{display:none}.panel-header{padding:20px 17px 15px}.panel-scroll{padding:14px}.map-tip{display:none}.gate-art{padding:35px}.gate-form{padding:25px}}
@media(max-width:650px){#gate{grid-template-columns:1fr}.gate-art{padding:22px;min-height:215px}.gate-art h1{font-size:37px;max-width:250px;margin:26px 0 10px}.gate-art p,.gate-land,.footer-note{display:none}.gate-art:after{content:'⛵';position:absolute;right:25px;bottom:30px;font-size:95px}.gate-form{padding:28px 23px;align-items:start}.gate-box h2{font-size:26px}.gate-box form{margin-top:15px}#app{height:100dvh;grid-template-columns:1fr;grid-template-rows:62px minmax(0,1fr) 69px}.topbar{gap:10px}.topbar .brand{display:none}.hud{gap:17px}.huditem small{font-size:7px}.huditem>span{font-size:17px}.huditem strong{font-size:12px}.topbar .savecontrol button{padding:8px;font-size:9px}.sidebar{grid-row:3;grid-column:1;flex-direction:row;justify-content:space-between;padding:4px 6px calc(3px + env(safe-area-inset-bottom));border-right:0;border-top:1px solid var(--line);z-index:10;gap:2px}.navbtn{min-width:38px;flex:1;padding:7px 2px;font-size:8px;gap:4px;border-radius:9px}.navbtn .navicon{font-size:20px}.workspace{grid-row:2;display:flex;flex-direction:column;overflow:auto}.map-section{flex:none;height:44dvh;min-height:280px;padding:13px 12px 10px}.map-heading{margin-bottom:10px}.map-heading h1{font-size:20px;margin-top:3px}.map-heading .eyebrow{font-size:8px}.weather{display:block;padding:6px 9px;font-size:9px}.map-wrap{min-height:180px;border-radius:13px}.goal-card{display:none}.map-overlay{left:10px;top:10px}.map-badge{font-size:8px;padding:6px 8px}.map-controls{bottom:9px;right:9px}.map-controls button{padding:6px 10px}.map-controls #zoomLabel{font-size:9px}.right-panel{flex:none;border-left:0;border-top:1px solid var(--line);overflow:visible}.panel-header{padding:16px 19px 10px;display:flex;align-items:center;justify-content:space-between}.panel-header h2{font-size:21px;margin:0}.panel-header .eyebrow{font-size:8px}.panel-scroll{overflow:visible;padding:16px 18px 25px}.crop-card{padding:11px}.toastarea{bottom:80px}.modal-backdrop{padding:14px}.modal{padding:22px}#placementHint{top:40px;left:10px;right:10px;font-size:10px;padding:8px}}
@media(prefers-reduced-motion:reduce){*{animation:none!important;transition:none!important}}
</style>
</head><body>
<section id="gate" aria-label="Masuk Harvest Harbor">
 <div class="gate-art"><div class="brand"><span class="brand-mark">♧</span><div><strong class="titlefont">harvest harbor</strong><small>GROW SOMETHING WONDERFUL</small></div></div><div><h1 class="titlefont">Kebun kecil.<br>Impian seluas<br>cakrawala.</h1><p>Tanam hari ini. Bangun kota pesisirmu, dan berlayarlah menuju kemungkinan baru.</p></div><div class="gate-land" aria-hidden="true"><span class="hill"></span><span class="art-house">🏡</span><span class="art-tree">🌳</span><span class="art-crop">🌾🌾🌾</span><span class="art-boat">⛵</span></div><div class="footer-note">PERTANIAN &nbsp; / &nbsp; KERAJINAN &nbsp; / &nbsp; PENJELAJAHAN</div></div>
 <div class="gate-form"><div class="gate-box" id="authBox"><div class="eyebrow">SELAMAT DATANG DI PESISIR</div><h2 class="titlefont">Menyiapkan pulau…</h2><div class="spinner"></div></div></div>
</section>
<section id="app" class="hidden" aria-label="Permainan Harvest Harbor">
 <header class="topbar"><div class="brand"><span class="brand-mark">♧</span><div><strong class="titlefont">harvest harbor</strong><small>KEBUNMU, CAKRAWALAMU</small></div></div><div id="hud" class="hud"></div><div class="savecontrol"><small id="saveStatus"></small><button class="secondary small" id="saveNow">☁ Simpan</button></div></header>
 <nav class="sidebar" id="nav" aria-label="Menu permainan"></nav>
 <main class="workspace"><section class="map-section"><div class="map-heading"><div><div class="eyebrow">KEPULAUAN HARVEST · PULAU PERTAMAMU</div><h1 class="titlefont" id="islandName">Kota pesisirmu</h1></div><div class="weather">☀ &nbsp; Hari yang cerah &nbsp; 24°</div></div><div class="map-wrap"><canvas id="world" tabindex="0" aria-label="Peta kota. Klik kebun untuk menanam, menyiram, atau memanen. Geser untuk melihat peta; gunakan tombol tambah dan kurang untuk zoom."></canvas><div class="map-overlay"><span class="map-badge">● &nbsp; PULAU AMBER</span><span class="map-badge" id="dayBadge">HARI 1</span></div><div id="placementHint" class="hidden"></div><div class="map-tip">Geser peta · Gulir untuk zoom · Klik untuk berinteraksi</div><div class="map-controls"><button id="zoomOut" aria-label="Perkecil peta">−</button><button id="zoomReset" aria-label="Pusatkan peta">⌖</button><button id="zoomIn" aria-label="Perbesar peta">+</button><span id="zoomLabel" class="map-badge">100%</span></div></div><div class="goal-card"><span class="goal-icon">🧭</span><div><div class="eyebrow">LANGKAH KECIL BERIKUTNYA</div><strong id="goalText"></strong><small>Tumbuh perlahan. Selalu ada hal baru di balik cakrawala.</small></div></div></section>
 <aside class="right-panel"><div class="panel-header"><div class="eyebrow" id="panelEyebrow">RAWAT, PANEN, ULANGI</div><h2 class="titlefont" id="panelTitle">Kebun</h2></div><div id="panel" class="panel-scroll"></div></aside></main>
</section>
<div id="toasts" class="toastarea" aria-live="polite"></div><div id="modalRoot"></div>
<script id="configuration">
/* ===== ISI TIGA NILAI DI BAWAH INI SEBELUM DIPUBLIKASIKAN =====
   Gunakan publishable key (sb_publishable_...), BUKAN secret / service_role.
   APP_URL wajib menyertakan path repository dan garis miring terakhir.
   Contoh: https://namapengguna.github.io/harvest-harbor/
*/
const SUPABASE_URL = '';
const SUPABASE_PUBLISHABLE_KEY = '';
const APP_URL = '';
</script>
<script id="engine">
/* Harvest Harbor — deterministic, bounded simulation. No network or DOM dependencies. */
const HH = (() => {
  'use strict';
  const VERSION=1, W=26, H=18, COAST=21, MAX_OFFLINE=28800;
  const ITEMS={
    wheat:['Gandum','🌾',7],corn:['Jagung','🌽',10],tomato:['Tomat','🍅',15],cane:['Tebu','🎋',13],cotton:['Kapas','☁️',16],berry:['Stroberi','🍓',36],
    seed_wheat:['Bibit gandum','🌱',3],seed_corn:['Bibit jagung','🌱',5],seed_tomato:['Bibit tomat','🌱',8],seed_cane:['Bibit tebu','🌱',7],seed_cotton:['Bibit kapas','🌱',9],seed_berry:['Bibit stroberi','🌱',16],
    wood:['Kayu','🪵',5],stone:['Batu','🪨',5],ore:['Bijih besi','⛰️',9],coal:['Batu bara','⚫',8],flour:['Tepung','🥣',19],bread:['Roti','🍞',52],feed:['Pakan','🌿',14],egg:['Telur','🥚',16],milk:['Susu','🥛',24],cheese:['Keju','🧀',67],sugar:['Gula','🧂',33],yarn:['Benang','🧵',38],cloth:['Kain','🧶',90],sail:['Layar','⛵',195],plank:['Papan','🪵',14],crate:['Peti','📦',36],iron:['Besi','🔩',32],tools:['Perkakas','🛠️',88],machine:['Komponen mesin','⚙️',188],component:['Komponen kapal','🔧',188],meal:['Hidangan','🍲',165],fertilizer:['Pupuk','🍃',8],rare:['Kristal laut','💎',100],vessel:['Kapal baru','🚢',0]
  };
  const CROPS={wheat:{time:24,yield:3},corn:{time:36,yield:3},tomato:{time:48,yield:3,harvest:12},cane:{time:50,yield:3,harvest:24},cotton:{time:60,yield:3,harvest:32},berry:{time:60,yield:4,tech:'greenhouse',green:true}};
  const B={
    warehouse:{name:'Gudang',icon:'📦',cost:90,mat:{wood:6},time:15,w:3,h:2,color:'#d2985b',desc:'120 ruang per level. Hasil tetap aman saat gudang penuh.'},
    house:{name:'Rumah pekerja',icon:'🏡',cost:85,mat:{wood:6,stone:3},time:16,desc:'Kapasitas 4 pekerja per level.'},
    well:{name:'Sumur',icon:'💧',cost:35,mat:{stone:4},time:12,w:1,h:1,desc:'Mengisi 1 air per 5 detik per level; menambah tangki 30.'},
    mill:{name:'Penggilingan',icon:'🌾',cost:100,mat:{wood:8,stone:5},time:20,need:{harvest:8},desc:'Mengolah gandum dan jagung menjadi tepung dan pakan.'},
    market:{name:'Pasar',icon:'🏪',cost:100,mat:{wood:8},time:20,need:{harvest:12},desc:'Pesanan penduduk. Bonus harga jual 2% per level.'},
    sawmill:{name:'Penggergajian',icon:'🪚',cost:110,mat:{wood:8,stone:5},time:22,need:{harvest:16},desc:'Kayu menjadi papan dan peti untuk pembangunan.'},
    coop:{name:'Kandang ayam',icon:'🐔',cost:120,mat:{wood:10,stone:4},time:25,need:{buildings:['mill']},desc:'3 ayam per level; 1 pakan dan 1 air per siklus 35 detik.'},
    bakery:{name:'Toko roti',icon:'🍞',cost:150,mat:{plank:6,stone:6},time:25,need:{buildings:['mill','sawmill']},desc:'Mengolah tepung menjadi roti bernilai lebih tinggi.'},
    silo:{name:'Silo',icon:'🌾',cost:140,mat:{plank:6,stone:8},time:25,need:{buildings:['mill']},desc:'100 ruang khusus gandum, jagung, dan pakan per level.'},
    barn:{name:'Kandang sapi',icon:'🐄',cost:180,mat:{plank:8,stone:6},time:30,need:{buildings:['coop']},desc:'2 sapi per level; 2 pakan dan 2 air per siklus 50 detik.'},
    dairy:{name:'Pengolahan susu',icon:'🧀',cost:200,mat:{plank:8,stone:8},time:30,need:{buildings:['barn']},desc:'Membuat keju dari susu.'},
    sugarworks:{name:'Pengolahan gula',icon:'🧂',cost:160,mat:{plank:8,stone:6},time:25,need:{harvest:24,buildings:['sawmill']},desc:'Mengolah tebu menjadi gula.'},
    textile:{name:'Pabrik tekstil',icon:'🧵',cost:220,mat:{plank:10,stone:8},time:30,need:{harvest:32,buildings:['sawmill']},desc:'Kapas → benang → kain → layar kapal.'},
    lab:{name:'Pusat riset',icon:'🔬',cost:200,mat:{plank:10,stone:10},time:30,need:{buildings:['mill','sawmill']},desc:'Peneliti menghasilkan poin riset. Studi manual juga tersedia.'},
    smelter:{name:'Peleburan',icon:'🔥',cost:240,mat:{plank:8,stone:15},time:35,need:{research:['metallurgy']},desc:'Mengolah bijih besi dan batu bara menjadi batangan besi.'},
    workshop:{name:'Bengkel',icon:'🛠️',cost:280,mat:{plank:10,iron:5},time:35,need:{buildings:['smelter']},desc:'Perkakas, komponen mesin, serta komponen kapal.'},
    greenhouse:{name:'Rumah kaca',icon:'🌿',cost:300,mat:{plank:10,iron:5},time:40,need:{research:['greenhouse']},color:'#80bfb0',desc:'4 petak per level; 50% lebih cepat. 2 air + 2 koin per tanaman.'},
    waterTower:{name:'Menara air',icon:'🚰',cost:250,mat:{plank:8,iron:6},time:30,need:{research:['irrigation']},desc:'Tangki +100 air per level, suplai irigasi otomatis.'},
    bank:{name:'Bank',icon:'🏦',cost:260,mat:{plank:10,stone:15},time:35,need:{sales:200},desc:'Simpanan, pinjaman, dan cicilan setiap 10 menit simulasi.'},
    restaurant:{name:'Restoran',icon:'🍲',cost:280,mat:{plank:12,stone:10},time:35,need:{buildings:['bakery','dairy']},desc:'Memasak hidangan. Pendapatan hanya saat hidangan dijual.'},
    power:{name:'Pembangkit listrik',icon:'⚡',cost:350,mat:{iron:10,tools:4},time:40,need:{research:['electricity']},desc:'1 batu bara per 60 detik aktif. Daya 4 per level.'},
    office:{name:'Kantor perdagangan',icon:'📜',cost:320,mat:{plank:12,iron:6},time:35,need:{research:['logistics']},desc:'Kontrak ekspor; level meningkatkan hadiah kontrak dan batas kredit.'},
    harbor:{name:'Pelabuhan',icon:'⚓',cost:500,mat:{plank:20,stone:20,iron:10,cloth:6},time:60,w:2,h:3,coastal:true,need:{research:['maritime']},color:'#65a4c3',desc:'2 tambatan kapal dan 50 ruang muatan per level. Wajib tepi laut.'},
    shipyard:{name:'Galangan kapal',icon:'⛵',cost:380,mat:{plank:16,iron:8,cloth:4},time:45,w:2,h:3,coastal:true,need:{buildings:['harbor']},desc:'Membangun, memperbaiki, dan meningkatkan kapal. Wajib tepi laut.'},
    portwarehouse:{name:'Gudang pelabuhan',icon:'🚢',cost:300,mat:{plank:12,iron:5},time:35,need:{buildings:['harbor']},desc:'Menambah 200 ruang muatan ekspor dan impor per level.'}
  };
  Object.values(B).forEach(d=>{d.w??=2;d.h??=2;d.color??='#d7795d';});
  const R={
    flour:{name:'Giling tepung',b:'mill',input:{wheat:2},out:{flour:1},time:12},feed:{name:'Giling pakan',b:'mill',input:{corn:2},out:{feed:2},time:14},
    bread:{name:'Panggang roti',b:'bakery',input:{flour:2},out:{bread:1},water:1,time:18},cheese:{name:'Buat keju',b:'dairy',input:{milk:2},out:{cheese:1},time:20},
    sugar:{name:'Olah gula',b:'sugarworks',input:{cane:2},out:{sugar:1},time:16},yarn:{name:'Pintal benang',b:'textile',input:{cotton:2},out:{yarn:1},time:18},
    cloth:{name:'Tenun kain',b:'textile',input:{yarn:2},out:{cloth:1},time:22},sail:{name:'Jahit layar',b:'textile',input:{cloth:2},out:{sail:1},time:26},
    plank:{name:'Potong papan',b:'sawmill',input:{wood:2},out:{plank:1},time:10},crate:{name:'Rakit peti',b:'sawmill',input:{plank:2},out:{crate:1},time:14},
    iron:{name:'Lebur besi',b:'smelter',input:{ore:2,coal:1},out:{iron:1},time:22},tools:{name:'Buat perkakas',b:'workshop',input:{plank:2,iron:1},out:{tools:1},time:24},
    machine:{name:'Rakit mesin',b:'workshop',input:{iron:2,tools:1},out:{machine:1},time:30,power:2,tech:'electricity'},
    component:{name:'Komponen kapal',b:'workshop',input:{iron:2,tools:1},out:{component:1},time:30},
    meal:{name:'Masak hidangan',b:'restaurant',input:{bread:1,cheese:1,tomato:1},out:{meal:1},water:1,time:25},
    vessel:{name:'Bangun kapal dagang',b:'shipyard',input:{plank:16,cloth:8,component:4},out:{vessel:1},coin:220,time:90,tech:'maritime'}
  };
  const TECH={
    seeds:{name:'Bibit unggul',branch:'Pertanian',rp:8,cost:35,time:20,mat:{},desc:'Hasil setiap panen bertambah 1; petani dapat menanam ulang.'},
    fertilizer:{name:'Pupuk efisien',branch:'Pertanian',rp:10,cost:45,time:25,pre:['seeds'],mat:{stone:4},desc:'Harga pupuk turun dari 8 menjadi 4 koin.'},
    greenhouse:{name:'Rumah kaca',branch:'Pertanian',rp:16,cost:90,time:35,pre:['seeds'],mat:{plank:4},desc:'Membuka rumah kaca dan stroberi.'},
    irrigation:{name:'Irigasi otomatis',branch:'Pertanian',rp:22,cost:120,time:40,pre:['greenhouse'],mat:{iron:3},desc:'Dengan menara air, semua tanaman disiram otomatis selama air tersedia.'},
    feedEff:{name:'Pakan efisien',branch:'Peternakan',rp:12,cost:60,time:25,mat:{corn:4},desc:'2 pakan hanya membutuhkan 1 jagung.'},
    metallurgy:{name:'Metalurgi',branch:'Industri',rp:8,cost:60,time:25,mat:{plank:3},desc:'Membuka peleburan besi.'},
    fastSmelt:{name:'Peleburan cepat',branch:'Industri',rp:18,cost:100,time:35,pre:['metallurgy'],mat:{iron:3},desc:'Peleburan bekerja 50% lebih cepat.'},
    queue:{name:'Antrean tambahan',branch:'Industri',rp:15,cost:80,time:30,mat:{plank:4},desc:'Menambah 3 slot antrean setiap fasilitas.'},
    storage:{name:'Gudang besar',branch:'Logistik',rp:18,cost:100,time:30,pre:['queue'],mat:{plank:6},desc:'Kapasitas gudang dan silo bertambah 50%.'},
    logistics:{name:'Distribusi cepat',branch:'Logistik',rp:18,cost:110,time:35,pre:['queue'],mat:{plank:4},desc:'Jalan memberi bonus kecepatan 35%; membuka kantor perdagangan.'},
    electricity:{name:'Kelistrikan',branch:'Industri',rp:24,cost:160,time:40,pre:['metallurgy','logistics'],mat:{iron:6,tools:2},desc:'Membuka pembangkit dan resep komponen mesin.'},
    maritime:{name:'Kapal dagang',branch:'Maritim',rp:25,cost:180,time:45,pre:['metallurgy','logistics'],mat:{cloth:4,tools:2},desc:'Membuka pelabuhan, galangan, dan kapal dagang.'},
    navigation:{name:'Navigasi',branch:'Maritim',rp:30,cost:220,time:50,pre:['maritime'],mat:{sail:1},desc:'Membuka Pulau Rempah setelah reputasi 10.'},
    engine:{name:'Mesin kapal',branch:'Maritim',rp:40,cost:320,time:60,pre:['navigation','electricity'],mat:{machine:2,rare:2},desc:'Kapal 30% lebih cepat; membuka Atol Mutiara pada reputasi 30.'}
  };
  const ROUTES={amber:{name:'Pulau Amber',time:70,cost:20,mult:1.65,rep:0,icon:'🏝️',goods:['bread','crate','cheese']},spice:{name:'Pulau Rempah',time:110,cost:35,mult:2.05,rep:10,tech:'navigation',icon:'🌴',goods:['cloth','sugar','tools']},pearl:{name:'Atol Mutiara',time:160,cost:60,mult:2.55,rep:30,tech:'engine',icon:'🪸',goods:['machine','meal','sail']}};
  const JOBS={farmer:'Petani',rancher:'Peternak',miner:'Penambang',crafter:'Pengrajin',researcher:'Peneliti',hauler:'Pengangkut'};
  const ACH={firstHarvest:['Panen pertama',20],firstBread:['Roti pertama',50],firstGreenhouse:['Kebun dalam kaca',70],firstResearch:['Rasa ingin tahu',50],firstBank:['Masa depan cerah',70],firstShip:['Kapten pertama',150],firstExport:['Menyapa cakrawala',200]};
  const copy=x=>JSON.parse(JSON.stringify(x)), sum=o=>Object.values(o).reduce((a,b)=>a+b,0), count=(s,k)=>s.inventory[k]||0;
  const fresh=(now=Date.now())=>({schemaVersion:VERSION,coins:200,savings:0,debt:0,bankClock:0,missed:0,water:30,rp:0,reputation:0,lastSim:Math.floor(now/1000)*1000,ticks:0,nextId:2,land:Array.from({length:12*12},(_,i)=>`${1+i%12},${2+Math.floor(i/12)}`),roads:[],inventory:{seed_wheat:8},portStock:{},plots:[{id:1,x:3,y:8},{id:2,x:4,y:8},{id:3,x:3,y:9},{id:4,x:4,y:9}].map(p=>({...p,soil:1,crop:null,progress:0,watered:false,fertilized:false,greenhouse:null})),nextPlot:5,buildings:[{id:1,type:'warehouse',x:3,y:4,level:1,left:0,upgradeLeft:0,queue:[],job:null,out:{},delivery:0,repeat:null,animals:0,feed:0,animalLeft:0,fuel:0}],workers:{hired:0,roles:{farmer:0,rancher:0,miner:0,crafter:0,researcher:0,hauler:0}},research:[],researchJob:null,ships:[],contracts:[],nextContract:1,achievements:[],stats:{harvest:0,sales:0,exports:0,crafted:0,income:0,expense:0,operating:0,produced:{}},ledger:[],gather:null,studyLeft:0,laborLeft:0,autoCrop:'wheat',settings:{sound:false}});
  const active=(s,t)=>s.buildings.filter(b=>b.type===t&&b.left<=0), levels=(s,t)=>active(s,t).reduce((n,b)=>n+b.level,0), has=(s,t)=>levels(s,t)>0, tech=(s,t)=>s.research.includes(t);
  function capacities(s){const silo=levels(s,'silo')*100*(tech(s,'storage')?1.5:1),grain=count(s,'wheat')+count(s,'corn')+count(s,'feed'),stored=Math.min(grain,silo);return {main:levels(s,'warehouse')*120*(tech(s,'storage')?1.5:1),used:sum(s.inventory)-stored,silo,siloUsed:stored,port:levels(s,'harbor')*50+levels(s,'portwarehouse')*200,portUsed:sum(s.portStock),workers:levels(s,'house')*4,water:30+levels(s,'well')*30+levels(s,'waterTower')*100,berths:levels(s,'harbor')*2};}
  function fits(s,items,port=false){if(port)return sum(s.portStock)+sum(items)<=capacities(s).port;const inv=s.inventory;s.inventory={...inv};for(const [k,v]of Object.entries(items))s.inventory[k]=(s.inventory[k]||0)+v;const c=capacities(s);s.inventory=inv;return c.used<=c.main;}
  function enough(s,items){return Object.entries(items).every(([k,v])=>count(s,k)>=v);}
  function take(s,items){if(!enough(s,items))throw Error('Bahan belum cukup.');for(const[k,v]of Object.entries(items))s.inventory[k]-=v;}
  function add(s,items,port=false){if(!fits(s,items,port))return false;const inv=port?s.portStock:s.inventory;for(const[k,v]of Object.entries(items))inv[k]=(inv[k]||0)+v;return true;}
  function log(s,label,amount,operating=false){s.ledger.unshift({at:s.lastSim,label,amount});s.ledger=s.ledger.slice(0,60);if(amount>0)s.stats.income+=amount;else{s.stats.expense-=amount;if(operating)s.stats.operating-=amount;}}
  function pay(s,n,label,op=false){if(s.coins<n)throw Error('Koin belum cukup. Jual hasil atau ambil pekerjaan harian.');s.coins-=n;log(s,label,-n,op);}
  function earn(s,n,label){s.coins+=n;log(s,label,n);}
  function achieve(s,k){if(!s.achievements.includes(k)){s.achievements.push(k);earn(s,ACH[k][1],`Pencapaian: ${ACH[k][0]}`);}}
  function needReason(s,n={}){const a=[];if(n.harvest&&s.stats.harvest<n.harvest)a.push(`panen ${n.harvest} hasil (${s.stats.harvest}/${n.harvest})`);if(n.sales&&s.stats.sales<n.sales)a.push(`penjualan ${n.sales} koin (${Math.floor(s.stats.sales)}/${n.sales})`);for(const t of n.research||[])if(!tech(s,t))a.push(`riset ${TECH[t].name}`);for(const t of n.buildings||[])if(!has(s,t))a.push(B[t].name);return a.join(' · ');}
  function cropReason(s,k,green=false){const d=CROPS[k];return d.green&&!green?'Khusus rumah kaca':d.tech&&!tech(s,d.tech)?`Riset ${TECH[d.tech].name}`:d.harvest>s.stats.harvest?`Panen ${d.harvest} hasil`:'';}
  function occupied(s,x,y,ignore=null){return s.buildings.some(b=>b.id!==ignore&&x>=b.x&&x<b.x+B[b.type].w&&y>=b.y&&y<b.y+B[b.type].h)||s.plots.some(p=>!p.greenhouse&&p.x===x&&p.y===y)||s.roads.includes(`${x},${y}`);}
  function placement(s,type,x,y,ignore=null){const d=B[type]||{w:1,h:1};if(!Number.isInteger(x)||!Number.isInteger(y)||x<0||y<0||x+d.w>COAST||y+d.h>H)return 'Lokasi berada di luar daratan.';if(d.coastal&&x+d.w!==COAST)return 'Bangunan ini harus menyentuh tepi laut di sisi kanan.';for(let xx=x;xx<x+d.w;xx++)for(let yy=y;yy<y+d.h;yy++){if(!s.land.includes(`${xx},${yy}`))return 'Beli seluruh petak lahan ini dahulu.';if(occupied(s,xx,yy,ignore))return 'Lokasi terhalang bangunan, jalan, atau kebun.';}return '';}
  function build(s,type,x,y){if(!B[type])throw Error('Bangunan tidak dikenal.');const d=B[type],why=needReason(s,d.need)||placement(s,type,x,y);if(why)throw Error(why);if(!enough(s,d.mat))throw Error('Material pembangunan belum cukup.');pay(s,d.cost,`Bangun ${d.name}`);take(s,d.mat);const b={id:s.nextId++,type,x,y,level:1,left:d.time,upgradeLeft:0,queue:[],job:null,out:{},delivery:0,repeat:null,animals:0,feed:0,animalLeft:0,fuel:0};s.buildings.push(b);return b;}
  function completeBuilding(s,b){if(b.type==='greenhouse'){const slots=b.level*4,saved=s.plots.filter(p=>p.greenhouse===b.id).length;for(let i=saved;i<slots;i++)s.plots.push({id:s.nextPlot++,x:b.x,y:b.y,greenhouse:b.id,soil:1,crop:null,progress:0,watered:false,fertilized:false});achieve(s,'firstGreenhouse');}if(b.type==='bank')achieve(s,'firstBank');}
  const getB=(s,id)=>{const b=s.buildings.find(b=>b.id===Number(id));if(!b)throw Error('Bangunan tidak ditemukan.');return b;};
  function upgradeCost(b){return {coins:Math.ceil(B[b.type].cost*.65*b.level),mat:Object.fromEntries(Object.entries(B[b.type].mat).map(([k,v])=>[k,Math.ceil(v*.5*b.level)])),time:15*b.level};}
  function upgrade(s,id){const b=getB(s,id),c=upgradeCost(b);if(b.left||b.upgradeLeft||b.level>=5)throw Error('Upgrade belum tersedia atau sudah level 5.');if(!enough(s,c.mat))throw Error('Material upgrade belum cukup.');pay(s,c.coins,'Upgrade '+B[b.type].name);take(s,c.mat);b.upgradeLeft=c.time;}
  function move(s,id,x,y){const b=getB(s,id),why=placement(s,b.type,x,y,id);if(why)throw Error(why);if(b.left||b.upgradeLeft)throw Error('Tunggu konstruksi selesai.');pay(s,Math.ceil(B[b.type].cost*.1),'Pindah bangunan');b.x=x;b.y=y;s.plots.filter(p=>p.greenhouse===b.id).forEach(p=>{p.x=x;p.y=y;});}
  function demolish(s,id){const b=getB(s,id);if(b.left||b.upgradeLeft||b.job||b.queue.length||sum(b.out)||b.animals||b.feed)throw Error('Selesaikan konstruksi dan produksi, kosongkan hasil, pakan, serta hewan dahulu.');if(s.plots.some(p=>p.greenhouse===b.id&&p.crop))throw Error('Panen semua tanaman rumah kaca dahulu.');if(b.type==='lab'&&s.researchJob)throw Error('Selesaikan riset dahulu.');if(b.type==='harbor'&&s.ships.length>(levels(s,'harbor')-b.level)*2)throw Error('Tambatan lain belum cukup untuk kapal yang ada.');const old=s.buildings;s.buildings=old.filter(x=>x.id!==b.id);const c=capacities(s);if(c.used>c.main||c.portUsed>c.port||s.workers.hired>c.workers){s.buildings=old;throw Error('Kapasitas setelah dibongkar tidak cukup. Kurangi stok atau pekerja dahulu.');}const refund=Object.fromEntries(Object.entries(B[b.type].mat).map(([k,v])=>[k,Math.floor(v*.5)]));if(!fits(s,refund)){s.buildings=old;throw Error('Sediakan ruang untuk pengembalian material.');}add(s,refund);earn(s,Math.floor(B[b.type].cost*.25),'Bongkar '+B[b.type].name);s.plots=s.plots.filter(p=>p.greenhouse!==b.id);s.water=Math.min(s.water,c.water);}
  const duration=(s,p)=>CROPS[p.crop].time/(1+.12*(p.soil-1))/(p.greenhouse?1.5:1)/(p.fertilized?1.35:1);
  function plant(s,p,k){if(p.crop)throw Error('Petak masih berisi tanaman.');if(!CROPS[k])throw Error('Tanaman tidak dikenal.');const reason=cropReason(s,k,!!p.greenhouse);if(reason)throw Error(reason);if(!enough(s,{['seed_'+k]:1}))throw Error('Bibit habis. Beli bibit di Pasar.');if(p.greenhouse){if(s.water<2)throw Error('Rumah kaca membutuhkan 2 air.');pay(s,2,'Operasional rumah kaca',true);s.water-=2;}take(s,{['seed_'+k]:1});p.crop=k;p.progress=0;p.watered=!!p.greenhouse;p.fertilized=false;}
  function water(s,p){if(!p.crop||p.watered)throw Error('Petak kosong atau sudah disiram.');if(s.water<1)throw Error('Air habis. Ambil air atau bangun sumur.');s.water--;p.watered=true;}
  function harvest(s,p){if(!p.crop||p.progress<duration(s,p))throw Error('Tanaman belum matang.');const n=CROPS[p.crop].yield+(tech(s,'seeds')?1:0),goods={[p.crop]:n};if(!add(s,goods))throw Error('Gudang penuh. Tanaman tetap aman sampai ada ruang.');s.stats.harvest+=n;p.crop=null;p.progress=0;p.watered=false;p.fertilized=false;achieve(s,'firstHarvest');}
  function plotAction(s,id,action,k){const p=s.plots.find(p=>p.id===Number(id));if(!p)throw Error('Petak tidak ditemukan.');if(action==='plant')plant(s,p,k);else if(action==='water')water(s,p);else if(action==='harvest')harvest(s,p);else if(action==='soil'){if(p.soil>=5)throw Error('Tanah sudah level 5.');pay(s,p.soil*25,'Perbaikan tanah');p.soil++;}else if(action==='fertilize'){if(!p.crop||p.fertilized)throw Error('Tanam dulu atau pupuk sudah digunakan.');take(s,{fertilizer:1});p.fertilized=true;}else if(action==='remove'){if(p.crop||p.greenhouse)throw Error('Panen dahulu. Petak rumah kaca tidak dapat dibongkar satuan.');s.plots=s.plots.filter(x=>x!==p);}else throw Error('Tindakan tidak dikenal.');}
  function buyLand(s,x,y){if(x<0||x>=COAST||y<0||y>=H||!Number.isInteger(x)||!Number.isInteger(y))throw Error('Pilih daratan.');const key=`${x},${y}`;if(s.land.includes(key))throw Error('Lahan sudah dimiliki.');pay(s,15,'Beli lahan');s.land.push(key);}
  function addPlot(s,x,y){const why=placement(s,'plot',x,y);if(why)throw Error(why);pay(s,30,'Perluas kebun');s.plots.push({id:s.nextPlot++,x,y,soil:1,crop:null,progress:0,watered:false,fertilized:false,greenhouse:null});}
  function road(s,x,y){const key=`${x},${y}`;if(s.roads.includes(key)){s.roads=s.roads.filter(r=>r!==key);earn(s,1,'Bongkar jalan');return;}const why=placement(s,'road',x,y);if(why)throw Error(why);pay(s,3,'Bangun jalan');s.roads.push(key);}
  function roadBonus(s,b){const near=s.roads.some(k=>{const[x,y]=k.split(',').map(Number);return x>=b.x-1&&x<=b.x+B[b.type].w&&y>=b.y-1&&y<=b.y+B[b.type].h;});return near?(tech(s,'logistics')?1.35:1.15):1;}
  function inputs(s,k){return k==='feed'&&tech(s,'feedEff')?{corn:1}:R[k].input;}
  const queueCap=(s,b)=>b.level+1+(tech(s,'queue')?3:0);
  function recipeReason(s,b,k){const r=R[k];if(!r||b.type!==r.b)return 'Resep tidak sesuai fasilitas.';if(b.left)return 'Konstruksi belum selesai.';if(r.tech&&!tech(s,r.tech))return 'Riset '+TECH[r.tech].name;if(k==='vessel'){const ordered=s.ships.length+s.buildings.reduce((n,x)=>n+x.queue.filter(q=>q==='vessel').length+(x.job?.recipe==='vessel'?1:0)+(x.out.vessel||0),0);if(ordered>=capacities(s).berths)return 'Tambatan penuh. Upgrade pelabuhan.';}return '';}
  function enqueue(s,id,k){const b=getB(s,id),why=recipeReason(s,b,k);if(why)throw Error(why);if(b.queue.length+(b.job?1:0)>=queueCap(s,b))throw Error('Antrean penuh. Upgrade bangunan atau riset antrean.');b.queue.push(k);startJob(s,b);}
  function startJob(s,b){if(b.left||b.job||sum(b.out)||!b.queue.length)return;const k=b.queue[0],r=R[k],mat=inputs(s,k);if(!enough(s,mat)||s.coins<(r.coin||0)||s.water<(r.water||0)||(r.power&&!has(s,'power')))return;take(s,mat);if(r.coin)pay(s,r.coin,'Produksi '+r.name);s.water-=r.water||0;b.queue.shift();b.job={recipe:k,left:r.time};}
  function collect(s,id){const b=getB(s,id);if(b.out.vessel){if(s.ships.length>=capacities(s).berths)return false;s.ships.push({id:s.nextId++,name:'Aurora '+(s.ships.length+1),level:1,condition:100,trip:null});b.out.vessel--;achieve(s,'firstShip');}const goods=Object.fromEntries(Object.entries(b.out).filter(([k,v])=>k!=='vessel'&&v>0));if(!sum(goods))return true;if(!add(s,goods))return false;for(const k of Object.keys(goods))b.out[k]=0;return true;}
  function buyAnimal(s,id){const b=getB(s,id),cap=b.type==='coop'?3:2;if(!['coop','barn'].includes(b.type)||b.left)throw Error('Kandang belum tersedia.');if(b.animals>=cap*b.level)throw Error('Kandang penuh.');pay(s,b.type==='coop'?35:70,'Beli ternak');b.animals++;}
  function feedAnimal(s,id,n=5){const b=getB(s,id);if(!['coop','barn'].includes(b.type)||!b.animals)throw Error('Beli ternak dahulu.');const amount=Math.min(n,count(s,'feed'),b.level*30-b.feed);if(amount<=0)throw Error('Pakan habis atau tempat pakan penuh.');take(s,{feed:amount});b.feed+=amount;}
  function sellAnimal(s,id){const b=getB(s,id);if(!b.animals||b.animalLeft||sum(b.out))throw Error('Selesaikan siklus dan kumpulkan hasil dahulu.');b.animals--;earn(s,b.type==='coop'?17:35,'Jual ternak');}
  function buy(s,k,n){if(!Object.hasOwn(ITEMS,k)||!Number.isInteger(n)||n<1||n>999||k==='vessel'||k==='rare')throw Error('Pembelian tidak valid.');if(k.startsWith('seed_')){const crop=k.slice(5);const why=cropReason(s,crop,CROPS[crop].green);if(why)throw Error(why);}const price=buyPrice(s,k);if(!fits(s,{[k]:n}))throw Error('Ruang gudang tidak cukup.');pay(s,price*n,'Beli '+ITEMS[k][0]);add(s,{[k]:n});}
  function buyPrice(s,k){return k==='fertilizer'&&tech(s,'fertilizer')?4:k.startsWith('seed_')||k==='fertilizer'?ITEMS[k][2]:Math.ceil(ITEMS[k][2]*2.2);}
  const sellPrice=(s,k)=>Math.floor(ITEMS[k][2]*(1+.02*Math.min(5,levels(s,'market'))));
  function sell(s,k,n){if(!Object.hasOwn(ITEMS,k)||k==='vessel'||!Number.isInteger(n)||n<1)throw Error('Jumlah penjualan tidak valid.');take(s,{[k]:n});const income=sellPrice(s,k)*n;earn(s,income,'Jual '+ITEMS[k][0]);s.stats.sales+=income;}
  function gather(s,k){if(s.gather)throw Error('Selesaikan pengumpulan sebelumnya.');if(!['wood','stone','ore','coal'].includes(k))throw Error('Sumber tidak dikenal.');s.gather={item:k,left:k==='wood'||k==='stone'?6:10,qty:3};}
  function hire(s){if(s.workers.hired>=capacities(s).workers)throw Error('Bangun atau upgrade rumah pekerja.');pay(s,25,'Rekrut pekerja');s.workers.hired++;}
  function assign(s,role,delta){if(!Object.hasOwn(JOBS,role)||![1,-1].includes(delta))throw Error('Penugasan tidak valid.');if(delta>0&&sum(s.workers.roles)>=s.workers.hired)throw Error('Tidak ada pekerja bebas.');if(s.workers.roles[role]+delta<0)throw Error('Tidak ada pekerja untuk dilepas.');s.workers.roles[role]+=delta;}
  function research(s,k){const t=TECH[k];if(!t||!has(s,'lab'))throw Error('Bangun pusat riset dahulu.');if(tech(s,k)||s.researchJob)throw Error('Riset sudah selesai atau sedang ada riset berjalan.');const why=needReason(s,{research:t.pre||[]});if(why)throw Error(why);if(s.rp<t.rp)throw Error('Poin riset belum cukup.');if(!enough(s,t.mat))throw Error('Material riset belum cukup.');pay(s,t.cost,'Riset '+t.name);take(s,t.mat);s.rp-=t.rp;s.researchJob={id:k,left:t.time};}
  function loanLimit(s){return Math.max(0,Math.floor(200+Math.min(s.stats.sales*.4,4000)+levels(s,'bank')*200+levels(s,'office')*100)-s.debt-s.missed*100);}
  function bank(s,action,n){if(!Number.isFinite(n)||n<=0||n>1e9)throw Error('Masukkan nominal positif.');n=Math.round(n*100)/100;if(action!=='repay'&&!has(s,'bank'))throw Error(needReason(s,B.bank.need)||'Bangun bank dahulu.');if(action==='deposit'){if(s.coins<n)throw Error('Koin tidak cukup.');s.coins-=n;s.savings+=n;}else if(action==='withdraw'){if(s.savings<n)throw Error('Simpanan tidak cukup.');s.savings-=n;s.coins+=n;}else if(action==='borrow'){if(n>loanLimit(s))throw Error('Melebihi batas pinjaman.');s.debt+=n;s.coins+=n;}else if(action==='repay'){n=Math.min(n,s.debt);if(s.coins<n)throw Error('Koin tidak cukup.');s.coins-=n;s.debt-=n;if(s.debt<=.01){s.debt=0;s.missed=0;}}else throw Error('Tindakan bank tidak dikenal.');}
  function bankTick(s){const dep=Math.floor(s.savings*.002*100)/100;s.savings+=dep;if(dep)log(s,'Bunga simpanan',dep);if(s.debt>0){const rate=.01/(1+.08*Math.max(0,levels(s,'bank')-1)),fee=Math.ceil(s.debt*rate*100)/100;s.debt+=fee;log(s,'Bunga pinjaman',-fee,true);const due=Math.min(s.debt,Math.max(10,s.debt*.1)),paid=Math.min(s.coins,due);s.coins-=paid;s.debt-=paid;if(paid+.01<due){s.missed++;const penalty=Math.min(20,2*s.missed);s.debt+=penalty;log(s,'Denda cicilan terlambat',-penalty,true);}else s.missed=Math.max(0,s.missed-1);if(s.debt<.01)s.debt=0;}}
  function transfer(s,k,n,toPort){if(!has(s,'harbor')||!Object.hasOwn(ITEMS,k)||!Number.isInteger(n)||n<1)throw Error('Pelabuhan atau jumlah belum valid.');const from=toPort?s.inventory:s.portStock;if((from[k]||0)<n)throw Error('Stok asal belum cukup.');if(!fits(s,{[k]:n},toPort))throw Error('Gudang tujuan penuh.');from[k]-=n;add(s,{[k]:n},toPort);}
  function routeReason(s,k){const r=ROUTES[k];return !r?'Rute tidak ditemukan.':r.tech&&!tech(s,r.tech)?'Riset '+TECH[r.tech].name:s.reputation<r.rep?`Reputasi ${r.rep} diperlukan`:'';}
  const shipCap=ship=>40+20*(ship.level-1), voyageTime=(s,ship,r)=>Math.ceil(r.time/(1+.2*(ship.level-1)+(tech(s,'engine')?.3:0)));
  function quote(s,ship,route,k,n,mode='trade',contractId=null){const r=ROUTES[route];if(!r||!Object.hasOwn(ITEMS,k)||!Number.isInteger(n)||n<1)throw Error('Muatan tidak valid.');const con=contractId?s.contracts.find(c=>c.id===Number(contractId)&&c.type==='export'&&c.status==='active'):null;if(contractId&&!con)throw Error('Kontrak tidak tersedia.');const cargo=mode==='expedition'?{crate:2,bread:2}:con?{[con.item]:con.qty}:{[k]:n};const reward=mode==='expedition'?30:con?con.reward:Math.floor(ITEMS[k][2]*n*(r.goods.includes(k)?r.mult:1.25));return{cargo,reward,rep:mode==='expedition'?10:con?con.rep:5,cost:r.cost,time:voyageTime(s,ship,r),goods:mode==='expedition'?{rare:2,ore:4}:{},contractId:con?.id||null};}
  function sendShip(s,id,route,k,n,mode='trade',contractId=null){const ship=s.ships.find(x=>x.id===Number(id));if(!ship||ship.trip||ship.condition<20||!has(s,'harbor'))throw Error('Kapal belum siap. Periksa perjalanan dan kondisi kapal.');if(!['trade','expedition'].includes(mode)||k==='vessel')throw Error('Jenis perjalanan tidak valid.');const why=routeReason(s,route);if(why)throw Error(why);const q=quote(s,ship,route,k,n,mode,contractId);if(sum(q.cargo)>shipCap(ship))throw Error('Muatan melebihi kapasitas kapal.');if(!Object.entries(q.cargo).every(([i,v])=>(s.portStock[i]||0)>=v))throw Error('Pindahkan muatan ke gudang pelabuhan dahulu.');pay(s,q.cost,'Biaya pelayaran',true);for(const[i,v]of Object.entries(q.cargo))s.portStock[i]-=v;ship.trip={...q,route,mode,left:q.time,status:'sailing'};if(q.contractId)s.contracts.find(c=>c.id===q.contractId).status='sailing';}
  function claimShip(s,id){const ship=s.ships.find(x=>x.id===Number(id)),t=ship?.trip;if(!t||t.status!=='ready')throw Error('Kapal belum kembali.');if(!fits(s,t.goods,true))throw Error('Kosongkan gudang pelabuhan untuk barang ekspedisi.');add(s,t.goods,true);earn(s,t.reward,'Hasil pelayaran');s.stats.sales+=t.reward;s.reputation+=t.rep;s.stats.exports++;if(t.contractId){const c=s.contracts.find(c=>c.id===t.contractId);if(c)c.status='completed';}ship.condition=Math.max(0,ship.condition-12);ship.trip=null;achieve(s,'firstExport');}
  function serviceShip(s,id,up=false){const ship=s.ships.find(x=>x.id===Number(id));if(!ship||ship.trip||!has(s,'shipyard'))throw Error('Kapal harus berlabuh dan galangan harus tersedia.');if(up&&ship.level>=5)throw Error('Kapal sudah level 5.');const mat=up?{plank:8,cloth:4,component:2}:{plank:2};if(!enough(s,mat))throw Error('Material belum cukup.');pay(s,up?150*ship.level:25,up?'Upgrade kapal':'Perbaikan kapal');take(s,mat);if(up)ship.level++;ship.condition=100;}
  function makeContracts(s){s.contracts=s.contracts.filter(c=>c.status==='sailing'||(c.status==='active'&&c.left>0));const town=['wheat','corn','tomato','bread','cheese','meal'].filter(k=>k==='wheat'||k==='corn'||count(s,k)>0||s.stats.produced[k]);while(s.contracts.filter(c=>c.type==='town').length<3){const serial=s.nextContract++,item=town[serial%town.length],qty=3+serial%4;s.contracts.push({id:serial,type:'town',item,qty,reward:Math.ceil(ITEMS[item][2]*qty*1.4),rep:1,left:300,status:'active'});}if(has(s,'office'))while(s.contracts.filter(c=>c.type==='export').length<3){const serial=s.nextContract++,item=['bread','cheese','cloth','tools','crate'][serial%5],qty=5+serial%4;s.contracts.push({id:serial,type:'export',item,qty,reward:Math.ceil(ITEMS[item][2]*qty*(2+.04*levels(s,'office'))),rep:8,left:600,status:'active'});}}
  function fulfill(s,id){if(!has(s,'market'))throw Error('Bangun pasar untuk menerima pesanan.');const c=s.contracts.find(c=>c.id===Number(id)&&c.type==='town'&&c.status==='active');if(!c||c.left<=0)throw Error('Pesanan sudah selesai atau kedaluwarsa.');take(s,{[c.item]:c.qty});c.status='completed';earn(s,c.reward,'Pesanan penduduk');s.stats.sales+=c.reward;s.reputation+=c.rep;}
  function operational(s,b){if(b.left)return 'Sedang dibangun';if(sum(b.out))return fits(s,Object.fromEntries(Object.entries(b.out).filter(([k])=>k!=='vessel')))?'Hasil menunggu distribusi':'Gudang penuh — jual atau pindahkan stok';if(b.job){const r=R[b.job.recipe];if(r.power&&powerAvailable(s)<r.power)return 'Listrik kurang — isi batu bara / tambah pembangkit';return 'Sedang memproduksi';}if(b.queue.length){const r=R[b.queue[0]];if(!enough(s,inputs(s,b.queue[0])))return 'Bahan kurang — cek resep dan Gudang';if(s.coins<(r.coin||0))return 'Koin kurang';if(s.water<(r.water||0))return 'Air kurang — ambil air / bangun sumur';if(r.power&&!has(s,'power'))return 'Bangun pembangkit listrik';return 'Menunggu produksi';}if(['coop','barn'].includes(b.type))return !b.animals?'Belum ada ternak':b.animalLeft?'Ternak sedang menghasilkan':b.feed<(b.type==='coop'?1:2)*b.animals?'Pakan kurang — giling jagung':s.water<(b.type==='coop'?1:2)*b.animals?'Air kurang':'Siap produksi';return 'Siap bekerja';}
  const powerAvailable=s=>active(s,'power').reduce((n,b)=>n+(b.fuel>0?b.level*4:0),0);
  function tick(s){s.ticks++;s.lastSim+=1000;for(const b of s.buildings){if(b.left>0){b.left--;if(!b.left)completeBuilding(s,b);}if(b.upgradeLeft>0){b.upgradeLeft--;if(!b.upgradeLeft){b.level++;completeBuilding(s,b);}}}
    const cap=capacities(s);if(s.ticks%5===0)s.water=Math.min(cap.water,s.water+levels(s,'well'));
    if(s.gather){if(s.gather.left>0)s.gather.left--;if(s.gather.left<=0&&add(s,{[s.gather.item]:s.gather.qty}))s.gather=null;}
    if(s.studyLeft>0){s.studyLeft--;if(s.studyLeft===0&&has(s,'lab'))s.rp++;}if(s.laborLeft>0){s.laborLeft--;if(!s.laborLeft)earn(s,25,'Pekerjaan dermaga');}
    if(s.ticks%30===0&&s.workers.roles.miner){for(const k of ['wood','stone','ore','coal'])add(s,{[k]:s.workers.roles.miner});}
    if(s.ticks%15===0&&has(s,'lab'))s.rp+=s.workers.roles.researcher*levels(s,'lab');
    if(s.researchJob&&has(s,'lab')){s.researchJob.left-=1+.1*s.workers.roles.researcher;if(s.researchJob.left<=0){s.research.push(s.researchJob.id);s.researchJob=null;achieve(s,'firstResearch');}}
    for(const p of s.plots){if(p.crop){if(!p.watered&&tech(s,'irrigation')&&has(s,'waterTower')&&s.water>0)water(s,p);if(p.watered)p.progress=Math.min(duration(s,p),p.progress+1);}}
    if(s.ticks%5===0&&s.workers.roles.farmer){let work=s.workers.roles.farmer*2;for(const p of s.plots){if(!work)break;try{if(p.crop&&p.progress>=duration(s,p)){harvest(s,p);work--;}else if(p.crop&&!p.watered){water(s,p);work--;}else if(!p.crop&&tech(s,'seeds')){plant(s,p,s.autoCrop);work--;}}catch{}}}
    const wantsPower=s.buildings.some(b=>b.job&&R[b.job.recipe].power);for(const b of active(s,'power')){if(wantsPower&&b.fuel<=0&&count(s,'coal')>0){take(s,{coal:1});b.fuel=60;}if(wantsPower&&b.fuel>0)b.fuel--;}
    let available=powerAvailable(s);
    for(const b of s.buildings){if(b.left)continue;if(sum(b.out)){b.delivery++;if(b.delivery>=(s.workers.roles.hauler?1:Math.ceil(3/roadBonus(s,b)))){collect(s,b.id);b.delivery=0;}if(sum(b.out))continue;}
      if(['coop','barn'].includes(b.type)){const n=b.type==='coop'?1:2;if(s.workers.roles.rancher&&b.feed<n*b.animals&&count(s,'feed')>0&&b.animals){try{feedAnimal(s,b.id,10);}catch{}}if(b.animalLeft>0){b.animalLeft-=1+.1*(b.level-1);if(b.animalLeft<=0){b.animalLeft=0;b.out[b.type==='coop'?'egg':'milk']=(b.out[b.type==='coop'?'egg':'milk']||0)+b.animals;}}else if(b.animals&&b.feed>=n*b.animals&&s.water>=n*b.animals){b.feed-=n*b.animals;s.water-=n*b.animals;b.animalLeft=b.type==='coop'?35:50;}continue;}
      if(!b.job){if(!b.queue.length&&b.repeat&&s.workers.roles.crafter&&b.repeat!=='vessel')b.queue.push(b.repeat);startJob(s,b);}
      if(b.job){const r=R[b.job.recipe];if(r.power){if(available<r.power)continue;available-=r.power;}b.job.left-=(1+.15*(b.level-1)+.05*s.workers.roles.crafter)*roadBonus(s,b)*(b.type==='smelter'&&tech(s,'fastSmelt')?1.5:1);if(b.job.left<=0){for(const[k,v]of Object.entries(r.out)){b.out[k]=(b.out[k]||0)+v;s.stats.produced[k]=(s.stats.produced[k]||0)+v;s.stats.crafted+=v;if(k==='bread')achieve(s,'firstBread');}b.job=null;b.delivery=0;}}
    }
    for(const ship of s.ships){if(ship.trip?.status==='sailing'){ship.trip.left=Math.max(0,ship.trip.left-1);if(!ship.trip.left)ship.trip.status='ready';}}
    for(const c of s.contracts)if(c.status==='active')c.left=Math.max(0,c.left-1);if(s.ticks%30===0||!s.contracts.length)makeContracts(s);
    s.bankClock++;if(s.bankClock>=600){s.bankClock=0;bankTick(s);}
  }
  function advance(s,now=Date.now()){const target=Math.floor(now/1000)*1000;if(target<=s.lastSim)return {seconds:0,capped:false};const gap=Math.floor((target-s.lastSim)/1000),seconds=Math.min(gap,MAX_OFFLINE);if(gap>MAX_OFFLINE)s.lastSim=target-seconds*1000;for(let i=0;i<seconds;i++)tick(s);return {seconds,capped:gap>MAX_OFFLINE};}
  function action(s,name,a={}){switch(name){case'build':return build(s,a.type,a.x,a.y);case'upgrade':return upgrade(s,a.id);case'move':return move(s,a.id,a.x,a.y);case'demolish':return demolish(s,a.id);case'plot':return plotAction(s,a.id,a.kind,a.crop);case'land':return buyLand(s,a.x,a.y);case'newplot':return addPlot(s,a.x,a.y);case'road':return road(s,a.x,a.y);case'buy':return buy(s,a.item,a.qty);case'sell':return sell(s,a.item,a.qty);case'gather':return gather(s,a.item);case'water':s.water=Math.min(capacities(s).water,s.water+10);return;case'labor':if(s.laborLeft)throw Error('Pekerjaan masih berlangsung.');s.laborLeft=60;return;case'enqueue':return enqueue(s,a.id,a.recipe);case'cancelQueue':{const b=getB(s,a.id);if(!Number.isInteger(a.index)||a.index<0||a.index>=b.queue.length)throw Error('Antrean tidak ditemukan.');b.queue.splice(a.index,1);return;}case'repeat':{const b=getB(s,a.id);if(a.recipe!==null&&(!R[a.recipe]||R[a.recipe].b!==b.type||a.recipe==='vessel'))throw Error('Resep pengulangan tidak valid.');b.repeat=a.recipe;return;}case'collect':if(!collect(s,a.id))throw Error('Gudang atau tambatan penuh.');return;case'animal':return buyAnimal(s,a.id);case'feed':return feedAnimal(s,a.id,a.qty);case'sellAnimal':return sellAnimal(s,a.id);case'returnFeed':{const b=getB(s,a.id);if(!add(s,{feed:b.feed}))throw Error('Gudang penuh.');b.feed=0;return;}case'hire':return hire(s);case'assign':return assign(s,a.role,a.delta);case'dismiss':if(s.workers.hired<=sum(s.workers.roles))throw Error('Lepas tugas pekerja dahulu.');s.workers.hired--;return;case'research':return research(s,a.tech);case'study':if(!has(s,'lab')||s.studyLeft)throw Error('Pusat riset belum siap atau studi masih berjalan.');s.studyLeft=12;return;case'bank':return bank(s,a.kind,a.qty);case'transfer':return transfer(s,a.item,a.qty,a.toPort);case'send':return sendShip(s,a.id,a.route,a.item,a.qty,a.mode,a.contractId);case'claim':return claimShip(s,a.id);case'service':return serviceShip(s,a.id,a.up);case'fulfill':return fulfill(s,a.id);default:throw Error('Tindakan tidak dikenal.');}}
  function validate(input){
    if(!input||typeof input!=='object'||Array.isArray(input))throw Error('State harus berupa objek.');const s=copy(input),fail=m=>{throw Error('Save tidak valid: '+m);},num=(v,min=0,max=1e12)=>typeof v==='number'&&Number.isFinite(v)&&v>=min&&v<=max,int=(v,min=0,max=1e9)=>num(v,min,max)&&Number.isInteger(v),obj=v=>v&&typeof v==='object'&&!Array.isArray(v),arr=(v,n)=>Array.isArray(v)&&v.length<=n,known=(dict,k)=>typeof k==='string'&&Object.hasOwn(dict,k),stock=v=>obj(v)&&Object.entries(v).every(([k,n])=>known(ITEMS,k)&&int(n,0,1e8)),unique=v=>new Set(v).size===v.length;
    const template=fresh();if(s.schemaVersion!==VERSION)fail('versi tidak didukung');if(Object.keys(s).some(k=>!Object.hasOwn(template,k))||Object.keys(template).some(k=>!Object.hasOwn(s,k)))fail('struktur state tidak cocok');
    for(const k of ['coins','savings','debt','water','rp','reputation','ticks','bankClock','missed','studyLeft','laborLeft'])if(!num(s[k]))fail(k);if(!int(s.bankClock,0,599)||!int(s.ticks)||!int(s.nextId,2)||!int(s.nextPlot,5)||!int(s.nextContract,1)||!num(s.lastSim,0,Date.now()+300000))fail('waktu / ID');
    if(!stock(s.inventory)||!stock(s.portStock)||s.inventory.vessel||s.portStock.vessel)fail('stok');
    const cell=k=>typeof k==='string'&&/^\d+,\d+$/.test(k)&&k.split(',').every((v,i)=>int(Number(v),0,(i?H:COAST)-1));
    if(!arr(s.land,COAST*H)||!s.land.every(cell)||!unique(s.land)||!arr(s.roads,COAST*H)||!s.roads.every(k=>s.land.includes(k))||!unique(s.roads))fail('lahan');
    if(!arr(s.buildings,150)||!unique(s.buildings.map(b=>b.id)))fail('bangunan');const occupiedCells=new Set(s.roads);
    for(const b of s.buildings){if(!obj(b)||!known(B,b.type)||!int(b.id,1)||!int(b.level,1,5)||!int(b.x,0,COAST-1)||!int(b.y,0,H-1)||!num(b.left,0,10000)||!num(b.upgradeLeft,0,10000)||!arr(b.queue,20)||!b.queue.every(k=>known(R,k)&&R[k].b===b.type)||!stock(b.out)||![b.delivery,b.animals,b.feed,b.animalLeft,b.fuel].every(v=>num(v,0,100000)))fail('struktur bangunan');if(b.repeat!==null&&(!known(R,b.repeat)||R[b.repeat].b!==b.type||b.repeat==='vessel'))fail('pengulangan resep');if(b.job!==null&&(!obj(b.job)||!known(R,b.job.recipe)||R[b.job.recipe].b!==b.type||!num(b.job.left,0,100000)))fail('pekerjaan');const d=B[b.type];if(b.x+d.w>COAST||b.y+d.h>H||(d.coastal&&b.x+d.w!==COAST))fail('lokasi bangunan');for(let x=b.x;x<b.x+d.w;x++)for(let y=b.y;y<b.y+d.h;y++){const k=`${x},${y}`;if(!s.land.includes(k)||occupiedCells.has(k))fail('bangunan bertumpuk / lahan belum dimiliki');occupiedCells.add(k);}if(!int(b.animals,0,(b.type==='coop'?3:b.type==='barn'?2:0)*b.level)||!int(b.feed,0,b.level*30))fail('kapasitas ternak');}
    if(!arr(s.plots,700)||!unique(s.plots.map(p=>p.id)))fail('petak');for(const p of s.plots){if(!int(p.id,1)||!int(p.soil,1,5)||!num(p.progress,0,1000)||typeof p.watered!=='boolean'||typeof p.fertilized!=='boolean'||(p.crop!==null&&!known(CROPS,p.crop)))fail('tanaman');if(p.greenhouse!==null){const b=s.buildings.find(b=>b.id===p.greenhouse&&b.type==='greenhouse');if(!b||s.plots.filter(x=>x.greenhouse===b.id).length>b.level*4||p.x!==b.x||p.y!==b.y)fail('rumah kaca');}else{const k=`${p.x},${p.y}`;if(!cell(k)||!s.land.includes(k)||occupiedCells.has(k)||p.crop==='berry')fail('lokasi petak');occupiedCells.add(k);}}
    if(!obj(s.workers)||!int(s.workers.hired,0,10000)||!obj(s.workers.roles)||Object.keys(s.workers.roles).length!==6||!Object.entries(s.workers.roles).every(([k,v])=>known(JOBS,k)&&int(v,0,10000))||sum(s.workers.roles)>s.workers.hired)fail('pekerja');
    if(!arr(s.research,100)||!unique(s.research)||!s.research.every(k=>known(TECH,k))||s.researchJob!==null&&(!obj(s.researchJob)||!known(TECH,s.researchJob.id)||!num(s.researchJob.left,0,10000)))fail('riset');
    if(!arr(s.ships,100)||!unique(s.ships.map(x=>x.id)))fail('kapal');for(const ship of s.ships){if(!int(ship.id,1)||typeof ship.name!=='string'||ship.name.length>40||!int(ship.level,1,5)||!num(ship.condition,0,100))fail('struktur kapal');const t=ship.trip;if(t!==null){if(!obj(t)||!known(ROUTES,t.route)||!['trade','expedition'].includes(t.mode)||!['sailing','ready'].includes(t.status)||!stock(t.cargo)||!stock(t.goods)||![t.left,t.reward,t.rep,t.cost,t.time].every(v=>num(v,0,1e8))||sum(t.cargo)>shipCap(ship)||(t.contractId!==null&&!int(t.contractId,1)))fail('perjalanan');}}
    if(!arr(s.contracts,200)||!unique(s.contracts.map(c=>c.id)))fail('kontrak');for(const c of s.contracts)if(!int(c.id,1)||!known(ITEMS,c.item)||!['town','export'].includes(c.type)||!['active','sailing','completed'].includes(c.status)||![c.qty,c.reward,c.rep,c.left].every(v=>num(v,0,1e8)))fail('struktur kontrak');
    if(!arr(s.achievements,20)||!unique(s.achievements)||!s.achievements.every(k=>known(ACH,k)))fail('pencapaian');if(!obj(s.stats)||!stock(s.stats.produced)||!['harvest','sales','exports','crafted','income','expense','operating'].every(k=>num(s.stats[k])))fail('statistik');if(!arr(s.ledger,60)||!s.ledger.every(l=>obj(l)&&typeof l.label==='string'&&l.label.length<=150&&num(l.at,0,99999999999999)&&num(l.amount,-1e12,1e12)))fail('buku kas');if(s.gather!==null&&(!obj(s.gather)||!['wood','stone','ore','coal'].includes(s.gather.item)||!num(s.gather.left,0,100)||!int(s.gather.qty,1,100)))fail('pengumpulan');if(!known(CROPS,s.autoCrop)||!obj(s.settings)||typeof s.settings.sound!=='boolean')fail('pengaturan');
    const c=capacities(s);if(s.workers.hired>c.workers||c.used>c.main||c.portUsed>c.port||s.ships.length>c.berths||s.water>c.water)fail('kapasitas');if(!unique([...s.ships,...s.buildings].map(x=>x.id))||s.nextId<=Math.max(0,...s.buildings.map(x=>x.id),...s.ships.map(x=>x.id))||s.nextPlot<=Math.max(0,...s.plots.map(x=>x.id))||s.nextContract<=Math.max(0,...s.contracts.map(x=>x.id)))fail('ID berulang');
    return s;
  }
  function parseSave(text){if(text.length>2e6)throw Error('File maksimum 2 MB.');const data=JSON.parse(text,(k,v)=>{if(['__proto__','prototype','constructor'].includes(k))throw Error('Kunci JSON tidak diizinkan.');return v;});if(data.game!=='Harvest Harbor'||data.schemaVersion!==VERSION)throw Error('File ini bukan save Harvest Harbor versi 1.');return validate(data.state);}
  function goal(s){if(!s.achievements.includes('firstHarvest'))return 'Tanam 4 petak gandum, siram, lalu panen dalam 24 detik.';if(!has(s,'mill'))return 'Kumpulkan 8 kayu + 5 batu, lalu bangun Penggilingan.';if(!has(s,'sawmill'))return 'Bangun Penggergajian untuk mengolah papan pembangunan.';if(!has(s,'lab'))return 'Buat 10 papan dan bangun Pusat Riset untuk membuka teknologi.';if(!tech(s,'metallurgy'))return 'Riset Metalurgi, bangun Peleburan, lalu produksi besi.';if(!tech(s,'maritime'))return 'Riset Antrean → Distribusi → Kapal dagang. Siapkan kain dan perkakas.';if(!has(s,'harbor'))return 'Perluas lahan ke tepi laut dan bangun Pelabuhan.';if(!s.ships.length)return 'Bangun Galangan dan rakit kapal: 16 papan, 8 kain, 4 komponen.';if(!s.stats.exports)return 'Kirim muatan ke gudang pelabuhan lalu berlayarlah ke Pulau Amber.';return 'Jelajahi kepulauan, tingkatkan armada, dan tumbuhkan kota pesisirmu.';}
  return {VERSION,W,H,COAST,MAX_OFFLINE,ITEMS,CROPS,B,R,TECH,ROUTES,JOBS,ACH,fresh,copy,sum,count,active,levels,has,tech,capacities,fits,enough,needReason,cropReason,placement,duration,upgradeCost,inputs,queueCap,recipeReason,buyPrice,sellPrice,loanLimit,routeReason,shipCap,voyageTime,quote,operational,powerAvailable,action,advance,validate,parseSave,goal};
})();
if(typeof module!=='undefined'&&module.exports)module.exports=HH;

</script>
<script id="application">
(() => {
 'use strict';
 const $=id=>document.getElementById(id),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>new Intl.NumberFormat('id-ID',{maximumFractionDigits:1}).format(n||0),time=n=>n>=3600?`${Math.floor(n/3600)}j ${Math.floor(n%3600/60)}m`:n>=60?`${Math.floor(n/60)}m ${Math.ceil(n%60)}d`:`${Math.max(0,Math.ceil(n))} detik`,money=n=>`🪙 ${fmt(n)}`;
 const NAV=[['Kebun','🌱','RAWAT, PANEN, ULANGI'],['Bangunan','🏡','BENTUK KOTA IMPIANMU'],['Produksi','⚙️','DARI BAHAN MENJADI KARYA'],['Gudang','📦','SETIAP HASIL PUNYA TEMPAT'],['Riset','🔬','BUKA KEMUNGKINAN BARU'],['Pasar','🧺','HASIL KEBUN, PELUANG BARU'],['Bank','🏦','RENCANAKAN HARI ESOK'],['Pelabuhan','⚓','ADA DUNIA DI LUAR SANA'],['Akun','☺','RUMAH UNTUK PROGRESMU']];
 let client=null,user=null,session=null,state=null,profile=null,revision=0,epoch=0,loading=false,recovering=false,dirty=false,changeSeq=0,pending=null,saving=null,conflict=false,connection='saved',offset=0,authBusy=false,saveTimer=null,simTimer=null,autoSaveTimer=null,localWarning=false,controller=null;
 let view='Kebun',selectedCrop='wheat',selectedPlot=null,selectedB=null,buildingTab='Bangun',buildingFilter='Semua',marketItem='seed_wheat',shipUI={id:null,route:'amber',item:'bread',qty:5,mode:'trade',contractId:null},transferItem='bread',modalResolve=null;
 const uid=()=>crypto.randomUUID(),tabId=uid();let restoreId=null;try{restoreId=sessionStorage.getItem('hh-tab');sessionStorage.setItem('hh-tab',tabId);}catch{}
 const draftKey=(id,tab=tabId)=>`hh:${id}:draft:${tab}`,now=()=>Date.now()+offset;
 function toast(message,bad=false){const el=document.createElement('div');el.className='toast'+(bad?' bad':'');el.textContent=message;$('toasts').append(el);setTimeout(()=>el.remove(),5500);}
 function chime(){if(!state?.settings.sound)return;try{const A=window.AudioContext||window.webkitAudioContext;if(!A)return;chime.ctx??=new A();chime.ctx.resume();const o=chime.ctx.createOscillator(),g=chime.ctx.createGain();o.connect(g);g.connect(chime.ctx.destination);o.frequency.value=650;g.gain.setValueAtTime(.06,chime.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.001,chime.ctx.currentTime+.15);o.start();o.stop(chime.ctx.currentTime+.16);}catch{}}
 function exportState(s=state){if(!s)return;const blob=new Blob([JSON.stringify({game:'Harvest Harbor',schemaVersion:HH.VERSION,state:s},null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`harvest-harbor-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Cadangan permainan diekspor.');}
 function ask(title,description,confirm='Lanjutkan',typed=null){if(modalResolve)modalResolve(false);return new Promise(resolve=>{modalResolve=resolve;$('modalRoot').innerHTML=`<div class="modal-backdrop"><div class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle"><div class="eyebrow">HARVEST HARBOR</div><h2 id="modalTitle" class="sectiongap">${esc(title)}</h2><p>${esc(description)}</p>${typed?`<label for="confirmText">Ketik ${esc(typed)} untuk melanjutkan</label><input id="confirmText" autocomplete="off">`:''}<div class="actions"><button class="ghost" id="modalCancel">Batal</button><button id="modalConfirm" ${typed?'disabled':''}>${esc(confirm)}</button></div></div></div>`;const finish=v=>{$('modalRoot').innerHTML='';modalResolve=null;resolve(v);};$('modalCancel').onclick=()=>finish(false);$('modalConfirm').onclick=()=>finish(true);if(typed){$('confirmText').oninput=e=>$('modalConfirm').disabled=e.target.value!==typed;$('confirmText').focus();}else $('modalCancel').focus();});}
 function checkpoint(){if(!user||!state)return;try{localStorage.setItem(draftKey(user.id),JSON.stringify({v:1,baseRevision:revision,dirty,state,pending,changeSeq,updated:Date.now()}));}catch{if(!localWarning){localWarning=true;toast('Cadangan lokal tidak dapat ditulis. Ekspor save dan periksa ruang penyimpanan browser.',true);}}}
 function readDraft(id){try{const raw=localStorage.getItem(draftKey(id))||(restoreId?localStorage.getItem(draftKey(id,restoreId)):null);if(!raw)return null;const d=JSON.parse(raw);if(d.v!==1||!Number.isSafeInteger(d.baseRevision)||!Number.isSafeInteger(d.changeSeq))return null;d.state=HH.validate(d.state);if(d.pending){if(typeof d.pending.writeId!=='string'||!Number.isSafeInteger(d.pending.expected)||!Number.isSafeInteger(d.pending.seq))throw Error('Cadangan pending rusak.');d.pending.state=HH.validate(d.pending.state);}return d;}catch{toast('Cadangan lokal tidak valid. Progres cloud tetap digunakan.',true);return null;}}
 function mark(important=false){dirty=true;changeSeq++;checkpoint();setStatus(navigator.onLine?'dirty':'offline');if(important){clearTimeout(saveTimer);saveTimer=setTimeout(syncNow,900);}}
 function setStatus(status){connection=status;const labels={saved:'Tersimpan',saving:'Sedang menyimpan',dirty:'Belum tersinkron',offline:'Koneksi terputus',conflict:'Konflik progres'};$('saveStatus').innerHTML=`<i class="statusdot ${status==='offline'||status==='conflict'?'offline':status==='dirty'?'dirty':''}"></i>${labels[status]||labels.dirty}`;$('saveNow').disabled=!!saving||conflict||!state;const inline=$('accountSaveStatus');if(inline)inline.textContent=labels[status]||labels.dirty;}
 function stopGame(){checkpoint();epoch++;clearInterval(simTimer);clearInterval(autoSaveTimer);clearTimeout(saveTimer);simTimer=autoSaveTimer=null;controller?.abort();controller=null;saving=null;state=null;pending=null;dirty=false;loading=false;conflict=false;changeSeq=0;if(modalResolve)modalResolve(false);modalResolve=null;$('modalRoot').innerHTML='';}
 async function rpc(name,args={},gen=epoch){const request=client.rpc(name,args);if(controller)request.abortSignal(controller.signal);const r=await request;if(gen!==epoch)throw Error('HH_STALE');if(r.error)throw r.error;return r.data;}
 async function syncNow(){if(!state||!user||conflict||recovering||loading||!dirty&&!pending)return;if(saving)return saving;if(!navigator.onLine){setStatus('offline');checkpoint();return;}const gen=epoch,owner=user.id;saving=(async()=>{try{
     if(!pending)pending={writeId:uid(),expected:revision,state:HH.copy(state),seq:changeSeq};checkpoint();setStatus('saving');const p=pending;
     const res=await rpc('hh_save_game',{p_expected_revision:p.expected,p_state:p.state,p_schema_version:HH.VERSION,p_write_id:p.writeId},gen);
     if(gen!==epoch||user?.id!==owner)return;if(!res.ok){conflict=true;setStatus('conflict');checkpoint();showConflict();return;}
     revision=Number(res.revision);pending=null;dirty=changeSeq!==p.seq;checkpoint();setStatus(dirty?'dirty':'saved');
   }catch(e){if(gen!==epoch||e.message==='HH_STALE')return;setStatus('offline');checkpoint();if(!/fetch|network|abort|timeout/i.test(e.message||''))toast('Cloud save gagal: '+humanError(e),true);}
   finally{if(gen===epoch){saving=null;$('saveNow').disabled=conflict||!state;if(connection==='saving')setStatus(dirty?'dirty':'saved');}}})();return saving;}
 function showConflict(){if(!state)return;const el=$('modalRoot');el.innerHTML=`<div class="modal-backdrop"><div class="modal" role="dialog" aria-modal="true" aria-labelledby="conflictTitle"><div class="eyebrow">PROGRES DARI PERANGKAT LAIN</div><h2 id="conflictTitle" class="sectiongap">Ada dua versi pulau.</h2><p>Progres cloud telah berubah sejak versi ini dimuat. Simulasi dijeda dan perubahan lokal tetap disimpan sebagai cadangan. Uang dan stok dari dua versi tidak digabungkan.</p><p>Ekspor cadangan untuk menyimpan versi ini, lalu muat versi cloud untuk melanjutkan.</p><div class="stack"><button id="exportConflict" class="secondary">Ekspor cadangan lokal</button><button id="loadConflict">Muat progres cloud</button></div><div id="conflictError" class="notice error sectiongap"></div></div></div>`;$('exportConflict').onclick=()=>exportState();$('loadConflict').onclick=async()=>{const btn=$('loadConflict');btn.disabled=true;try{const gen=epoch,data=await rpc('hh_load_game',{},gen);if(!data.save)throw Error('Save cloud tidak ditemukan. Cadangan lokal tetap aman.');const next=HH.validate(data.save.state);checkpoint();state=next;revision=Number(data.save.revision);pending=null;dirty=false;changeSeq=0;conflict=false;offset=Date.parse(data.server_time)-Date.now();$('modalRoot').innerHTML='';runLoaded();}catch(e){if($('conflictError'))$('conflictError').textContent=humanError(e);btn.disabled=false;}};}
 function runLoaded(){if(!state)return;const result=HH.advance(state,now());if(result.seconds){mark();if(result.seconds>10)toast(`Selamat kembali! Progres ${time(result.seconds)} dihitung${result.capped?' (batas 8 jam)':''}.`);}checkpoint();$('gate').classList.add('hidden');$('app').classList.remove('hidden');$('islandName').textContent=`Pulau ${profile?.player_name||'Harapan'}`;renderNav();renderPanel();renderHud();resetCamera();setStatus(dirty?'dirty':'saved');clearInterval(simTimer);clearInterval(autoSaveTimer);simTimer=setInterval(()=>{if(!state||conflict||recovering)return;const res=HH.advance(state,now());if(res.seconds){dirty=true;changeSeq++;if(state.ticks%5===0)checkpoint();if(!saving)setStatus(navigator.onLine?'dirty':'offline');renderHud();if(state.ticks%2===0&&!$('modalRoot').children.length&&!['INPUT','SELECT','TEXTAREA'].includes(document.activeElement?.tagName))renderPanel();}},1000);autoSaveTimer=setInterval(syncNow,20000);}
 async function loadAccount(sess,force=false){if(!sess)return;if(!force&&user?.id===sess.user.id&&(state||loading))return;stopGame();const gen=epoch;session=sess;user=sess.user;loading=true;controller=new AbortController();$('app').classList.add('hidden');showGate('loading');try{
    let data=await rpc('hh_load_game',{},gen);if(!data.save)data=await rpc('hh_initialize',{p_player_name:user.user_metadata?.player_name||'Petani'},gen);
    const cloud=data.save;if(!cloud||cloud.schema_version!==HH.VERSION)throw Error('Versi save cloud tidak didukung.');state=HH.validate(cloud.state);revision=Number(cloud.revision);profile=data.profile||{player_name:'Petani'};offset=Date.parse(data.server_time)-Date.now();if(!Number.isFinite(offset))offset=0;
    const draft=readDraft(user.id);if(draft?.dirty||draft?.pending){if(draft.baseRevision===revision||draft.pending?.writeId===cloud.last_write_id){state=draft.state;changeSeq=draft.changeSeq;dirty=true;if(draft.pending?.writeId===cloud.last_write_id){pending=null;}else pending=draft.pending||null;}else{state=draft.state;revision=draft.baseRevision;changeSeq=draft.changeSeq;pending=draft.pending||null;dirty=true;conflict=true;}}
    try{const stored=localStorage.getItem(`hh-view-${user.id}`),urlView=new URL(location.href).searchParams.get('panel');view=NAV.some(n=>n[0]===urlView)?urlView:NAV.some(n=>n[0]===stored)?stored:'Kebun';}catch{view='Kebun';}
    loading=false;selectedB=null;selectedPlot=null;if(conflict){$('gate').classList.add('hidden');$('app').classList.remove('hidden');renderNav();renderHud();renderPanel();resetCamera();setStatus('conflict');showConflict();}else runLoaded();
   }catch(e){if(gen!==epoch)return;loading=false;state=null;showGate('loadError',humanError(e));}}
 function humanError(e){const m=e?.message||String(e);if(/Invalid login credentials/i.test(m))return 'Email atau password tidak sesuai.';if(/Email not confirmed/i.test(m))return 'Email belum diverifikasi. Buka email verifikasi atau kirim ulang tautannya.';if(/rate limit|too many|over_email_send_rate_limit/i.test(m))return 'Terlalu banyak permintaan. Tunggu beberapa saat lalu coba lagi.';if(/fetch|network|timeout|abort/i.test(m))return 'Tidak dapat terhubung. Periksa internet, URL Supabase, lalu coba kembali.';if(/email address.*invalid|invalid.*email/i.test(m))return 'Alamat email tidak valid.';if(/password.*least|weak_password/i.test(m))return 'Gunakan password yang lebih kuat, minimal 8 karakter.';if(/permission|row-level|schema cache|function.*exist|relation.*exist/i.test(m))return 'Periksa apakah seluruh supabase_setup.sql sudah dijalankan pada proyek yang benar.';if(/expired|invalid.*token/i.test(m))return 'Tautan atau sesi telah kedaluwarsa. Minta tautan baru atau login kembali.';return m;}
 function showGate(mode='login',message=''){
   $('gate').classList.remove('hidden');$('app').classList.add('hidden');const titles={login:'Selamat datang kembali.',register:'Awal dari cerita besarmu.',forgot:'Kembali ke kebunmu.',resend:'Periksa kotak masukmu.',recover:'Buat password baru.',loading:'Menyiapkan pulau…',config:'Satu langkah sebelum bermain.',loadError:'Pulau belum dapat dimuat.'};let content=`<div class="eyebrow">SELAMAT DATANG DI PESISIR</div><h2 class="titlefont">${titles[mode]||titles.login}</h2>`;
   if(mode==='loading'){content+='<p class="muted">Memeriksa sesi dan memuat progres cloud.</p><div class="spinner"></div>';}
   else if(mode==='config'){content+=`<p class="muted">Isi konfigurasi proyek di bagian atas kode aplikasi, lalu muat ulang halaman ini.</p><div class="notice">${esc(message)}</div><div class="card sectiongap"><strong>Yang perlu disiapkan</strong><ol class="mini-sub"><li>Jalankan supabase_setup.sql.</li><li>Isi SUPABASE_URL dan SUPABASE_PUBLISHABLE_KEY.</li><li>Isi APP_URL dengan alamat GitHub Pages lengkap.</li><li>Atur URL redirect dan email di Supabase.</li></ol><small>Petunjuk lengkap ada di PANDUAN.md dalam paket unduhan.</small></div><button class="full sectiongap" data-auth-mode="reload">Muat ulang</button>`;}
   else if(mode==='loadError'){content+=`<div class="notice error">${esc(message)}</div><p class="online-note">Permainan baru tidak dibuat saat koneksi gagal. Progres yang sudah ada tetap aman.</p><div class="stack sectiongap"><button id="retryLoad">Coba muat kembali</button><button class="ghost" id="gateLogout">Logout</button></div>`;}
   else{
    if(['login','register'].includes(mode))content+=`<p class="muted">${mode==='login'?'Kebun dan kotamu siap dilanjutkan.':'Mulai dengan 200 koin, 4 petak, dan 8 bibit gandum.'}</p><div class="gate-tabs"><button data-auth-mode="login" class="${mode==='login'?'active':''}">Masuk</button><button data-auth-mode="register" class="${mode==='register'?'active':''}">Daftar</button></div>`;
    if(mode==='forgot')content+='<p class="muted">Kami akan mengirim tautan untuk membuat password baru.</p>';if(mode==='resend')content+='<p class="muted">Masukkan email pendaftaran untuk mengirim ulang verifikasi.</p>';
    content+=`<div id="authNotice" class="notice ${message?'':'hidden'}">${esc(message)}</div><form id="authForm" data-mode="${mode}">${mode==='register'?'<label for="playerName">Nama pemain</label><input id="playerName" name="name" maxlength="32" minlength="2" placeholder="Nama untuk pulau dan ceritamu" autocomplete="nickname" required>':''}${mode!=='recover'?'<label for="email">Email</label><input id="email" name="email" type="email" placeholder="nama@email.com" autocomplete="email" required>':''}${['login','register','recover'].includes(mode)?`<label for="password">${mode==='recover'?'Password baru':'Password'}</label><input id="password" name="password" type="password" minlength="8" maxlength="128" placeholder="Minimal 8 karakter" autocomplete="${mode==='login'?'current-password':'new-password'}" required>`:''}${['register','recover'].includes(mode)?'<label for="confirmPassword">Konfirmasi password</label><input id="confirmPassword" name="confirm" type="password" minlength="8" maxlength="128" autocomplete="new-password" required>':''}<button type="submit" class="full sectiongap">${{login:'Masuk ke pulau →',register:'Mulai petualangan →',forgot:'Kirim tautan pemulihan',resend:'Kirim ulang verifikasi',recover:'Simpan password baru'}[mode]}</button></form><div class="gate-links">${mode==='login'?'<button data-auth-mode="forgot">Lupa password?</button><button data-auth-mode="resend">Kirim ulang verifikasi</button>':mode==='recover'?'<button id="gateLogout">Batalkan dan logout</button>':'<button data-auth-mode="login">← Kembali ke login</button>'}</div><p class="checkline">☁ &nbsp; Satu akun. Pulau yang sama di setiap perangkat.</p>`;
   }
   $('authBox').innerHTML=content;document.querySelectorAll('[data-auth-mode]').forEach(b=>b.onclick=()=>b.dataset.authMode==='reload'?location.reload():showGate(b.dataset.authMode));if($('authForm'))$('authForm').onsubmit=submitAuth;if($('retryLoad'))$('retryLoad').onclick=()=>loadAccount(session,true);if($('gateLogout'))$('gateLogout').onclick=logout;
 }
 async function submitAuth(e){e.preventDefault();if(authBusy||!client)return;const form=e.currentTarget,mode=form.dataset.mode,fd=new FormData(form),email=String(fd.get('email')||'').trim(),password=String(fd.get('password')||'');if(['register','recover'].includes(mode)&&password!==fd.get('confirm')){authNotice('Konfirmasi password tidak sama.',true);return;}const name=String(fd.get('name')||'').trim();if(mode==='register'&&name.length<2){authNotice('Nama pemain minimal 2 karakter.',true);return;}authBusy=true;form.querySelectorAll('button,input').forEach(x=>x.disabled=true);try{let res;
   if(mode==='login'){res=await client.auth.signInWithPassword({email,password});if(res.error)throw res.error;await loadAccount(res.data.session);}
   else if(mode==='register'){res=await client.auth.signUp({email,password,options:{data:{player_name:name},emailRedirectTo:APP_URL}});if(res.error)throw res.error;if(res.data.session)await loadAccount(res.data.session);else showGate('login','Pendaftaran diterima. Buka email verifikasi, klik tautannya, lalu login. Periksa juga folder spam.');}
   else if(mode==='forgot'){const redirect=new URL(APP_URL);redirect.searchParams.set('recovery','1');res=await client.auth.resetPasswordForEmail(email,{redirectTo:redirect.href});if(res.error)throw res.error;authNotice('Jika email terdaftar, tautan pemulihan akan dikirim. Periksa kotak masuk dan spam.');}
   else if(mode==='resend'){res=await client.auth.resend({type:'signup',email,options:{emailRedirectTo:APP_URL}});if(res.error)throw res.error;authNotice('Jika email perlu diverifikasi, tautan baru akan dikirim.');}
   else if(mode==='recover'){res=await client.auth.updateUser({password});if(res.error)throw res.error;recovering=false;try{sessionStorage.removeItem('hh-recovery');}catch{}const url=new URL(location.href);url.searchParams.delete('recovery');url.hash='';history.replaceState({},'',url);toast('Password diperbarui.');const latest=await client.auth.getSession();if(latest.error)throw latest.error;await loadAccount(latest.data.session,true);}
   }catch(err){authNotice(humanError(err),true);}finally{authBusy=false;if(form.isConnected)form.querySelectorAll('button,input').forEach(x=>x.disabled=false);}}
 function authNotice(message,bad=false){const el=$('authNotice');if(!el){toast(message,bad);return;}el.className='notice'+(bad?' error':'');el.textContent=message;}
 async function logout(){await syncNow();stopGame();showGate('loading');const {error}=await client.auth.signOut({scope:'local'});if(error){showGate('loadError','Logout belum berhasil: '+humanError(error));return;}user=session=null;recovering=false;try{sessionStorage.removeItem('hh-recovery');}catch{}showGate('login');}
 async function init(){
   if(!SUPABASE_URL||!SUPABASE_PUBLISHABLE_KEY||!APP_URL){showGate('config','Konfigurasi Supabase dan alamat aplikasi belum diisi.');return;}
   try{if(!/^https:\/\//.test(SUPABASE_URL)||!SUPABASE_PUBLISHABLE_KEY.startsWith('sb_publishable_'))throw Error('Gunakan HTTPS dan publishable key dengan awalan sb_publishable_.');const app=new URL(APP_URL);if(app.protocol!=='https:'&&!['localhost','127.0.0.1'].includes(app.hostname))throw Error('APP_URL harus menggunakan HTTPS.');if(!app.pathname.endsWith('/')||app.search||app.hash)throw Error('APP_URL harus diakhiri / dan tidak memuat query atau hash.');
    const callback=new URL(location.href),hash=new URLSearchParams(callback.hash.slice(1));recovering=callback.searchParams.get('recovery')==='1'||hash.get('type')==='recovery';try{recovering=recovering||sessionStorage.getItem('hh-recovery')==='1';if(recovering)sessionStorage.setItem('hh-recovery','1');}catch{}
    await new Promise((resolve,reject)=>{if(window.supabase){resolve();return;}const script=document.createElement('script');script.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.57.4/dist/umd/supabase.js';script.crossOrigin='anonymous';const timer=setTimeout(()=>reject(Error('SDK Supabase belum termuat. Periksa koneksi dan akses CDN.')),15000);script.onload=()=>{clearTimeout(timer);resolve();};script.onerror=()=>{clearTimeout(timer);reject(Error('SDK Supabase gagal dimuat. Periksa koneksi dan akses CDN.'));};document.head.append(script);});
    const fetchTimed=async(input,options={})=>{const ctrl=new AbortController(),timer=setTimeout(()=>ctrl.abort(),15000),upstream=options.signal;const onAbort=()=>ctrl.abort();if(upstream?.aborted)ctrl.abort();else upstream?.addEventListener('abort',onAbort,{once:true});try{return await fetch(input,{...options,signal:ctrl.signal});}finally{clearTimeout(timer);upstream?.removeEventListener('abort',onAbort);}};
    client=window.supabase.createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true,flowType:'implicit'},global:{fetch:fetchTimed}});
    client.auth.onAuthStateChange((event,sess)=>{session=sess;if(event==='PASSWORD_RECOVERY'){recovering=true;try{sessionStorage.setItem('hh-recovery','1');}catch{}}setTimeout(()=>{if(recovering){if(state)stopGame();showGate('recover');return;}if(event==='SIGNED_OUT'){stopGame();user=null;showGate('login');}else if(sess&&['SIGNED_IN','INITIAL_SESSION'].includes(event))loadAccount(sess);},0);});
    const {data,error}=await client.auth.getSession();if(error)throw error;session=data.session;if(callback.searchParams.get('error')||hash.get('error')){showGate('forgot',decodeURIComponent(hash.get('error_description')||callback.searchParams.get('error_description')||'Tautan sudah tidak berlaku. Minta tautan baru.'));return;}
    if(recovering){showGate('recover',!session?'Tautan pemulihan belum menghasilkan sesi. Jika tautan kedaluwarsa, minta tautan baru.':'');return;}if(session)await loadAccount(session);else showGate('login');
   }catch(e){showGate('config',humanError(e));}
 }
 function button(label,act,data={},cls='secondary small',disabled=false){return `<button class="${cls}" data-action="${act}" ${Object.entries(data).map(([k,v])=>`data-${k}="${esc(v)}"`).join(' ')} ${disabled?'disabled':''}>${label}</button>`;}
 function mat(items){return Object.entries(items).map(([k,v])=>`<span class="${HH.count(state,k)<v?'missing':''}">${HH.ITEMS[k][1]} ${v} ${HH.ITEMS[k][0]} <span class="mini">(${fmt(HH.count(state,k))})</span></span>`).join(' · ');}
 const progress=(v,max)=>`<div class="progress"><i style="width:${Math.min(100,Math.max(0,v/max*100))}%"></i></div>`;
 function renderNav(){$('nav').innerHTML=NAV.map(([name,icon])=>`<button class="navbtn ${view===name?'active':''}" data-view="${name}" aria-current="${view===name?'page':'false'}"><span class="navicon">${icon}</span>${name}</button>`).join('');}
 function setView(v){if(!NAV.some(n=>n[0]===v))return;view=v;try{localStorage.setItem(`hh-view-${user.id}`,v);const url=new URL(location.href);url.searchParams.set('panel',v);history.replaceState({},'',url);}catch{}renderNav();renderPanel(true);}
 function renderHud(){if(!state)return;const c=HH.capacities(state),items=[['🪙',fmt(state.coins),'Koin'],['👤',`${state.workers.hired}/${c.workers}`,'Pekerja'],['📦',`${fmt(c.used)}/${fmt(c.main)}`,'Gudang'],['✧',fmt(state.rp),'Riset']];if(HH.has(state,'well')||HH.has(state,'waterTower'))items.push(['💧',`${fmt(state.water)}/${c.water}`,'Air']);if(HH.tech(state,'electricity'))items.push(['⚡',HH.powerAvailable(state),'Listrik']);$('hud').innerHTML=items.map(([i,n,t])=>`<div class="huditem"><span>${i}</span><div><strong>${n}</strong><small>${t}</small></div></div>`).join('');$('goalText').textContent=HH.goal(state);$('dayBadge').textContent=`HARI ${1+Math.floor(state.ticks/600)}`;}
 function renderPanel(top=false){if(!state)return;const scroll=$('panel').scrollTop;$('panelTitle').textContent=view;$('panelEyebrow').textContent=NAV.find(n=>n[0]===view)[2];const renderers={Kebun:farmPanel,Bangunan:buildPanel,Produksi:productionPanel,Gudang:storagePanel,Riset:researchPanel,Pasar:marketPanel,Bank:bankPanel,Pelabuhan:harborPanel,Akun:accountPanel};$('panel').innerHTML=renderers[view]();$('panel').scrollTop=top?0:scroll;bindForms();}
 function farmPanel(){const s=state,ps=s.plots.filter(p=>!p.greenhouse),chosen=CROPTEXT(selectedCrop);let html=`<p class="panel-intro">Pilih bibit, lalu klik petak di peta. Klik lagi untuk menyiram dan memanen.</p><div class="row between"><h3>Pilih tanaman</h3><span class="pill">${s.plots.length} PETAK</span></div>`;
  for(const[k,d]of Object.entries(HH.CROPS)){const why=HH.cropReason(s,k,!!d.green);html+=`<button class="crop-card full ${selectedCrop===k?'selected':''}" data-action="selectCrop" data-crop="${k}" ${why?'disabled':''}><span class="bigicon">${HH.ITEMS[k][1]}</span><span class="grow" style="text-align:left"><strong>${HH.ITEMS[k][0]}</strong><small>${why?esc(why):`${d.time} detik · hasil ${d.yield+(HH.tech(s,'seeds')?1:0)} · jual ${money(HH.sellPrice(s,k))}${d.green?' · rumah kaca':''}`}</small></span><span class="pill gold">${HH.count(s,'seed_'+k)} bibit</span><span class="radio"></span></button>`;}
  html+=`<div class="grid2 sectiongap">${button('🌱 Tanam semua','farmAll',{kind:'plant'})}${button('💧 Siram semua','farmAll',{kind:'water'})}${button('🧺 Panen matang','farmAll',{kind:'harvest'})}${button('＋ Petak · 30 koin','place',{type:'plot'})}</div><div class="row between sectiongap"><h3>Petak kebun</h3><small>${chosen}</small></div><div class="plotlist">${ps.map(plotButton).join('')}</div>`;
  for(const b of HH.active(s,'greenhouse'))html+=`<h3 class="sectiongap">🌿 Rumah kaca #${b.id}</h3><small>4 petak/level · 2 air + 2 koin/tanaman · tumbuh 50% lebih cepat</small><div class="plotlist sectiongap">${s.plots.filter(p=>p.greenhouse===b.id).map(plotButton).join('')}</div>`;
  if(selectedPlot){const p=s.plots.find(x=>x.id===selectedPlot);if(p)html+=`<div class="card sectiongap"><strong>Petak #${p.id} · Tanah Lv.${p.soil}</strong><p class="mini-sub">${p.crop?`${HH.ITEMS[p.crop][0]} · ${p.watered?'sudah disiram':'butuh air'} · ${p.progress>=HH.duration(s,p)?'siap panen':time(HH.duration(s,p)-p.progress)}`:'Siap ditanami'}</p><div class="actions">${button('🍃 Pupuk (1)','plot',{id:p.id,kind:'fertilize'},'secondary small',!p.crop||p.fertilized)}${button(`Tanah + · ${25*p.soil} koin`,'plot',{id:p.id,kind:'soil'},'secondary small',p.soil>=5)}${button('Hapus petak','plot',{id:p.id,kind:'remove'},'ghost small',!!p.crop||!!p.greenhouse)}</div></div>`;}
  html+=`<div class="card sectiongap"><div class="row between"><h3>💧 Persediaan air</h3><strong>${fmt(s.water)}/${HH.capacities(s).water}</strong></div><small>Ambil air manual tanpa biaya. Sumur mengisinya otomatis.</small><div class="actions">${button('Ambil 10 air','water')}${button('Beli bibit & pupuk →','view',{view:'Pasar'})}</div></div><div class="notice sectiongap">${esc(HH.goal(s))}</div>`;return html;}
 const CROPTEXT=k=>HH.ITEMS[k][0];
 function plotButton(p){const ripe=p.crop&&p.progress>=HH.duration(state,p);return `<button class="plotbtn ${ripe?'ripe':''}" data-action="plotClick" data-id="${p.id}">${p.crop?HH.ITEMS[p.crop][1]:'＋'}<small>#${p.id} · ${!p.crop?'tanam':!p.watered?'siram':ripe?'panen':time(HH.duration(state,p)-p.progress)}</small></button>`;}
 function workerPanel(){const s=state,c=HH.capacities(s);return `<p class="panel-intro">Rumah membatasi jumlah pekerja. Aktivitas dasar tetap dapat dikerjakan manual.</p><div class="statgrid"><div class="statbox"><small>Jumlah pekerja</small><strong>${s.workers.hired}/${c.workers}</strong></div><div class="statbox"><small>Belum ditugaskan</small><strong>${s.workers.hired-HH.sum(s.workers.roles)}</strong></div></div><div class="actions">${button('＋ Rekrut · 25 koin','hire')}${button('Lepas pekerja bebas','dismiss',{},'ghost small')}</div><div class="sectiongap">${Object.entries(HH.JOBS).map(([k,n])=>`<div class="worker-row"><div><strong>${n}</strong><div class="mini-sub">${{farmer:'Panen & siram; tanam ulang setelah Bibit unggul',rancher:'Mengisi pakan otomatis dari gudang',miner:'+1 tiap sumber daya / 30 detik / pekerja',crafter:'Mempercepat dan mengulang resep pilihan',researcher:'Poin riset tiap 15 detik di pusat riset',hauler:'Distribusi hasil menjadi 1 detik'}[k]}</div></div><div class="row">${button('−','assign',{role:k,delta:-1})}<strong>${s.workers.roles[k]}</strong>${button('+','assign',{role:k,delta:1})}</div></div>`).join('')}</div><label for="autoCrop">Bibit untuk petani otomatis</label><select id="autoCrop">${Object.keys(HH.CROPS).filter(k=>!HH.CROPS[k].green).map(k=>`<option value="${k}" ${s.autoCrop===k?'selected':''}>${HH.ITEMS[k][0]}</option>`).join('')}</select><p class="online-note">Petani menggunakan bibit, air, dan ruang gudang yang benar-benar tersedia. Peneliti membutuhkan Pusat Riset yang telah selesai.</p>`;}
 function buildPanel(){const tabs=['Bangun','Milikmu','Pekerja'];let html=`<div class="filterrow">${tabs.map(t=>button(t,'buildingTab',{tab:t},buildingTab===t?'small':'ghost small')).join('')}</div>`;if(buildingTab==='Pekerja')return html+workerPanel();if(buildingTab==='Milikmu'){return html+(state.buildings.length?state.buildings.map(b=>`<div class="card build-card"><div class="row"><span class="bigicon">${HH.B[b.type].icon}</span><div class="grow"><strong>${HH.B[b.type].name}</strong><div class="mini-sub">Lv.${b.level} · (${b.x}, ${b.y}) · ${esc(HH.operational(state,b))}</div></div></div><div class="actions">${button('Buka bangunan →','selectB',{id:b.id})}</div></div>`).join(''):'');}
  html+=`<p class="panel-intro">Pilih bangunan, lalu pilih lahan hijau di peta. Pelabuhan dan galangan ditempatkan tepat di tepi laut kanan.</p><div class="grid2">${button('＋ Lahan · 15 koin','place',{type:'land'})}${button('Jalan · 3 koin','place',{type:'road'})}</div><p class="online-note">Jalan di samping bangunan memberi bonus kecepatan 15%, atau 35% setelah riset Distribusi.</p><label for="buildingFilter">Tampilkan</label><select id="buildingFilter"><option ${buildingFilter==='Semua'?'selected':''}>Semua</option><option ${buildingFilter==='Terbuka'?'selected':''}>Terbuka</option><option ${buildingFilter==='Terkunci'?'selected':''}>Terkunci</option></select><div class="spacer"></div>`;
  for(const[k,d]of Object.entries(HH.B)){const why=HH.needReason(state,d.need);if(buildingFilter==='Terbuka'&&why||buildingFilter==='Terkunci'&&!why)continue;html+=`<div class="card build-card ${why?'locked':''}"><div class="row"><span class="bigicon">${d.icon}</span><div class="grow"><strong>${d.name}</strong><div class="mini-sub">${d.w} × ${d.h} petak · ${time(d.time)}</div></div><span class="price">${money(d.cost)}</span></div><p>${d.desc}</p><div class="material">${mat(d.mat)}</div><div class="row between sectiongap">${why?`<small>🔒 ${esc(why)}</small>`:`<small>${HH.active(state,k).length} dimiliki</small>`}${button(why?'Terkunci':'Bangun ＋','place',{type:k},why?'ghost small':'small',!!why)}</div></div>`;}return html;
 }
 function buildingDetail(b){const s=state,d=HH.B[b.type],u=HH.upgradeCost(b),workers=HH.R[Object.keys(HH.R).find(k=>HH.R[k].b===b.type)]?s.workers.roles.crafter:b.type==='lab'?s.workers.roles.researcher:['coop','barn'].includes(b.type)?s.workers.roles.rancher:b.type==='greenhouse'?s.workers.roles.farmer:0;return `<div class="card"><div class="row"><span class="bigicon">${d.icon}</span><div><h3>${d.name}</h3><span class="pill">LEVEL ${b.level} / 5</span></div></div><p class="mini-sub sectiongap">${d.desc}</p><small>${esc(HH.operational(s,b))} · Pekerja bersama: ${workers}</small>${b.left?progress(d.time-b.left,d.time)+`<small>Konstruksi: ${time(b.left)}</small>`:''}${b.upgradeLeft?`<small>Upgrade selesai dalam ${time(b.upgradeLeft)}</small>`:''}<div class="actions">${button(`Upgrade · ${money(u.coins)}`,'upgrade',{id:b.id},'secondary small',b.level>=5||!!b.left||!!b.upgradeLeft)}${button(`Pindah · ${Math.ceil(d.cost*.1)} koin`,'moveB',{id:b.id},'ghost small',!!b.left||!!b.upgradeLeft)}${button('Bongkar','demolish',{id:b.id},'danger small')}</div>${b.level<5?`<div class="material sectiongap">Upgrade: ${mat(u.mat)} · ${time(u.time)}</div>`:''}<small class="mini-sub">Setiap level meningkatkan kapasitas atau kecepatan fasilitas.</small></div>`;}
 function productionPanel(){const s=state;let b=s.buildings.find(x=>x.id===selectedB);if(!b)b=s.buildings.find(x=>Object.values(HH.R).some(r=>r.b===x.type)||['coop','barn'].includes(x.type))||s.buildings[0];if(!b)return '<div class="empty">Bangun fasilitas dahulu.</div>';selectedB=b.id;let html=`<label for="productionBuilding" style="margin-top:0">Fasilitas</label><select id="productionBuilding">${s.buildings.map(x=>`<option value="${x.id}" ${x.id===b.id?'selected':''}>${HH.B[x.type].icon} ${HH.B[x.type].name} #${x.id} · Lv.${x.level}</option>`).join('')}</select><div class="spacer"></div>${buildingDetail(b)}`;
  if(HH.sum(b.out)){html+=`<div class="notice sectiongap"><strong>Hasil tersimpan di bangunan</strong><div class="material">${Object.entries(b.out).filter(([,v])=>v).map(([k,v])=>`${HH.ITEMS[k][1]} ${v} ${HH.ITEMS[k][0]}`).join(' · ')}</div><div class="actions">${button('Kumpulkan hasil','collect',{id:b.id})}</div></div>`;}
  if(['coop','barn'].includes(b.type)){const chicken=b.type==='coop',n=chicken?1:2;html+=`<div class="statgrid"><div class="statbox"><small>${chicken?'Ayam':'Sapi'}</small><strong>${b.animals}/${(chicken?3:2)*b.level}</strong></div><div class="statbox"><small>Pakan di kandang</small><strong>${b.feed}/${b.level*30}</strong></div></div><p class="mini-sub">Setiap siklus: ${n*b.animals} pakan + ${n*b.animals} air → ${b.animals} ${chicken?'telur':'susu'}. Waktu ${chicken?35:50} detik, lebih cepat pada level tinggi.</p>${b.animalLeft?progress((chicken?35:50)-b.animalLeft,chicken?35:50)+`<small>Selesai ${time(b.animalLeft)}</small>`:''}<div class="actions">${button(`Beli ${chicken?'ayam · 35':'sapi · 70'} koin`,'animal',{id:b.id},'small',!!b.left)}${button('Isi 5 pakan','feed',{id:b.id,qty:5})}${button('Kembalikan pakan','returnFeed',{id:b.id},'ghost small')}${button('Jual 1 ternak','sellAnimal',{id:b.id},'ghost small')}</div>`;}
  const recipes=Object.entries(HH.R).filter(([,r])=>r.b===b.type);if(recipes.length){html+=`<div class="row between sectiongap"><h3>Antrean produksi</h3><span class="pill">${b.queue.length+(b.job?1:0)} / ${HH.queueCap(s,b)}</span></div>`;if(b.job){const r=HH.R[b.job.recipe];html+=`<div class="card"><strong>${r.name}</strong>${progress(r.time-b.job.left,r.time)}<small>Sisa kerja ${time(b.job.left)} · ${esc(HH.operational(s,b))}</small></div>`;}html+=b.queue.map((k,i)=>`<div class="queue-item"><span>${i+1}. ${HH.R[k].name}</span>${button('×','cancelQueue',{id:b.id,index:i},'ghost small')}</div>`).join('');if(!b.job&&!b.queue.length)html+='<p class="mini-sub">Pilih resep untuk memulai. Bahan dipotong sekali saat pekerjaan mulai.</p>';for(const[k,r]of recipes){const why=HH.recipeReason(s,b,k),out=Object.entries(r.out).map(([i,v])=>`${v} ${HH.ITEMS[i][0]}`).join(',');html+=`<div class="recipe"><div class="row between"><strong>${r.name}</strong><small>${time(r.time)}</small></div><div class="material sectiongap">${mat(HH.inputs(s,k))}${r.coin?` · ${money(r.coin)}`:''}${r.water?` · 💧 ${r.water}`:''}${r.power?` · ⚡ ${r.power}`:''}</div><p class="mini-sub" style="margin:8px 0">→ ${out} ${k!=='vessel'?`· nilai jual ${money(Object.entries(r.out).reduce((v,[i,n])=>v+HH.sellPrice(s,i)*n,0))}`:''}</p>${why?`<small>🔒 ${esc(why)}</small>`:''}<div class="actions">${button('＋ Antrekan','enqueue',{id:b.id,recipe:k},'small',!!why)}${k!=='vessel'?button(b.repeat===k?'↻ Otomatis aktif':'↻ Ulang otomatis','repeat',{id:b.id,recipe:b.repeat===k?'':k},b.repeat===k?'small':'ghost small'):''}</div></div>`;}html+='<p class="online-note">Ulang otomatis membutuhkan pengrajin. Hasil dikirim setelah 3 detik; pengangkut mempercepatnya. Saat gudang penuh, hasil tetap di fasilitas dan pekerjaan berikutnya berhenti.</p>';}
  if(b.type==='lab')html+=`<div class="actions sectiongap">${button('Buka riset →','view',{view:'Riset'})}</div>`;if(b.type==='bank')html+=`<div class="actions sectiongap">${button('Buka bank →','view',{view:'Bank'})}</div>`;if(b.type==='power')html+=`<div class="notice sectiongap">${b.level*4} daya · bahan bakar tersisa ${time(b.fuel)}. Pembangkit mengambil batu bara dari gudang saat industri listrik bekerja.</div>`;if(b.type==='house')html+=`<div class="actions sectiongap">${button('Atur pekerja →','buildingTab',{tab:'Pekerja'})}</div>`;return html;
 }
 function storagePanel(){const s=state,c=HH.capacities(s);let html=`<p class="panel-intro">Stok dipakai bersama oleh seluruh fasilitas. Silo memberi ruang khusus untuk gandum, jagung, dan pakan.</p><div class="card"><div class="row between"><strong>📦 Gudang utama</strong><small>${fmt(c.used)} / ${fmt(c.main)}</small></div>${progress(c.used,c.main)}<div class="row between"><small>Silo</small><small>${fmt(c.siloUsed)} / ${fmt(c.silo)}</small></div>${c.silo?progress(c.siloUsed,c.silo):''}<div class="row between"><small>Gudang pelabuhan</small><small>${fmt(c.portUsed)} / ${fmt(c.port)}</small></div></div><h3 class="sectiongap">Bahan dari alam</h3><p class="mini-sub">Hutan dan tambang selalu dapat dikumpulkan kembali. Pengumpulan berhenti menunggu bila gudang penuh.</p><div class="grid2">${['wood','stone','ore','coal'].map(k=>button(`${HH.ITEMS[k][1]} ${HH.ITEMS[k][0]} +3`,'gather',{item:k},'secondary small',!!s.gather)).join('')}</div>${s.gather?`<div class="notice sectiongap">${HH.ITEMS[s.gather.item][0]}: ${s.gather.left?time(s.gather.left):'menunggu ruang gudang'}</div>`:''}<h3 class="sectiongap">Isi gudang</h3>${inventoryList(s.inventory,true)}${c.port?`<h3 class="sectiongap">Muatan pelabuhan</h3>${inventoryList(s.portStock,false)}`:''}`;return html;}
 function inventoryList(inv,sellable){const entries=Object.entries(inv).filter(([,v])=>v>0);return entries.length?entries.map(([k,v])=>`<div class="inventory-row"><span>${HH.ITEMS[k][1]}</span><div>${HH.ITEMS[k][0]}<small>Jual ${money(HH.sellPrice(state,k))} / unit</small></div><div><strong>${fmt(v)}</strong>${sellable?`<div class="actions" style="margin-top:4px">${button('Jual 1','sell',{item:k,qty:1},'ghost small')}${button('Semua','sell',{item:k,qty:v},'ghost small')}</div>`:''}</div></div>`).join(''):'<div class="empty">Belum ada barang di sini.</div>';}
 function researchPanel(){const s=state;let html=`<p class="panel-intro">Peneliti menghasilkan poin tiap 15 detik. Studi manual tetap tersedia setelah Pusat Riset dibangun.</p><div class="card"><div class="row between"><h3>✧ ${fmt(s.rp)} poin riset</h3><span class="pill">${s.research.length}/${Object.keys(HH.TECH).length}</span></div><div class="actions">${button(s.studyLeft?`Studi · ${time(s.studyLeft)}`:'Studi manual · +1 poin','study',{},'secondary small',!!s.studyLeft||!HH.has(s,'lab'))}${button('Atur peneliti','buildingTab',{tab:'Pekerja'},'ghost small')}</div></div>${!HH.has(s,'lab')?'<div class="notice sectiongap">Bangun Pusat Riset: perlu Penggilingan dan Penggergajian, 200 koin, 10 papan, 10 batu.</div>':''}`;
  if(s.researchJob){const t=HH.TECH[s.researchJob.id];html+=`<div class="card sectiongap"><strong>Meneliti ${t.name}</strong>${progress(t.time-s.researchJob.left,t.time)}<small>${time(s.researchJob.left)} tersisa</small></div>`;}
  for(const[k,t]of Object.entries(HH.TECH)){const done=HH.tech(s,k),why=HH.needReason(s,{research:t.pre||[]});html+=`<div class="card build-card sectiongap"><div class="techbranch">${t.branch}</div><div class="row between"><strong>${done?'✓ ':''}${t.name}</strong><span class="pill ${done?'':'gold'}">${done?'SELESAI':t.rp+' RP'}</span></div><p>${t.desc}</p>${!done?`<div class="material">${money(t.cost)} · ${time(t.time)}<br>${mat(t.mat)}</div>${why?`<small>🔒 ${why}</small>`:''}<div class="actions">${button('Mulai riset','research',{tech:k},'small',!!why||!!s.researchJob||!HH.has(s,'lab'))}</div>`:''}</div>`;}return html;
 }
 function marketPanel(){const s=state,supplies=[...Object.keys(HH.CROPS).map(k=>'seed_'+k),'fertilizer','wood','stone','ore','coal'];let html=`<p class="panel-intro">Jual hasil dari gudang, beli perlengkapan, atau penuhi pesanan untuk nilai lebih tinggi.</p><div class="card"><h3>Perlengkapan kebun</h3><label for="marketItem">Barang</label><select id="marketItem">${supplies.map(k=>`<option value="${k}" ${marketItem===k?'selected':''}>${HH.ITEMS[k][1]} ${HH.ITEMS[k][0]} · ${HH.buyPrice(s,k)} koin</option>`).join('')}</select><label for="buyQty">Jumlah</label><div class="row"><input id="buyQty" type="number" min="1" max="999" value="5">${button('Beli','buySupply',{},'')}</div><p class="online-note">Bibit dan pupuk dijual sesuai harga daftar. Pemasok material lebih mahal daripada mengumpulkan sendiri.</p></div><div class="card sectiongap"><h3>Selalu ada jalan untuk tumbuh</h3><small>Pekerjaan dermaga memberi 25 koin dalam 60 detik, tanpa modal. Bisa diulangi, satu pekerjaan pada satu waktu.</small><div class="actions">${button(s.laborLeft?`Bekerja · ${time(s.laborLeft)}`:'Kerja dermaga · +25 koin','labor',{},'secondary small',!!s.laborLeft)}</div></div><h3 class="sectiongap">Pesanan penduduk</h3>`;
  if(!HH.has(s,'market'))html+='<div class="notice">Bangun Pasar setelah panen 12 hasil untuk mengerjakan pesanan.</div>';for(const c of s.contracts.filter(c=>c.type==='town'&&c.status==='active'))html+=`<div class="contract"><div class="row between"><strong>${HH.ITEMS[c.item][1]} ${c.qty} ${HH.ITEMS[c.item][0]}</strong><span class="price">${money(c.reward)}</span></div><div class="row between sectiongap"><small>+${c.rep} reputasi · ${time(c.left)}</small>${button('Kirim','fulfill',{id:c.id},'small',!HH.has(s,'market'))}</div></div>`;
  return html+`<h3 class="sectiongap">Jual persediaan</h3>${inventoryList(s.inventory,true)}`;
 }
 function bankPanel(){const s=state,bank=HH.has(s,'bank'),rate=1/(1+.08*Math.max(0,HH.levels(s,'bank')-1));return `<p class="panel-intro">Kelola modal pembangunan dengan simpanan dan pinjaman. Setiap periode bank = 10 menit waktu simulasi.</p>${!bank?`<div class="notice">${esc(HH.needReason(s,HH.B.bank.need)||'Syarat terpenuhi. Bangun Bank melalui menu Bangunan.')}.</div>`:''}<div class="statgrid"><div class="statbox"><small>Tunai</small><strong>${money(s.coins)}</strong></div><div class="statbox"><small>Simpanan</small><strong>${money(s.savings)}</strong></div><div class="statbox"><small>Utang tersisa</small><strong>${money(s.debt)}</strong></div><div class="statbox"><small>Batas pinjaman baru</small><strong>${money(HH.loanLimit(s))}</strong></div></div><div class="card"><h3>Transaksi bank</h3><label for="bankAmount">Nominal koin</label><input id="bankAmount" type="number" min="1" max="1000000000" value="100" step="1"><div class="grid2 sectiongap">${button('Setor simpanan','bank',{kind:'deposit'},'secondary small',!bank)}${button('Tarik simpanan','bank',{kind:'withdraw'},'secondary small',!bank)}${button('Ambil pinjaman','bank',{kind:'borrow'},'secondary small',!bank)}${button('Bayar utang','bank',{kind:'repay'},'secondary small',!s.debt)}</div></div><div class="card sectiongap"><h3>Jadwal & bunga</h3><div class="log-row"><span>Periode berikutnya</span><strong>${time(600-s.bankClock)}</strong></div><div class="log-row"><span>Bunga simpanan / periode</span><strong>0,2%</strong></div><div class="log-row"><span>Bunga pinjaman / periode</span><strong>${fmt(rate)}%</strong></div><p class="mini-sub sectiongap">Cicilan otomatis: 10% utang (minimal 10 koin, maksimal sisa utang). Kekurangan tetap menjadi utang; keterlambatan menambah denda 2 koin × jumlah keterlambatan, maksimal 20 koin/periode, dan mengurangi batas pinjaman. Pembongkaran bank tidak menghapus utang. Pelunasan menghapus penalti kredit.</p>${s.missed?`<div class="notice error">${s.missed} cicilan terlambat. Jual barang atau kerja dermaga, lalu bayar utang.</div>`:''}</div><h3 class="sectiongap">Ringkasan ekonomi</h3><div class="card"><div class="log-row"><span>Total pemasukan</span><strong>${money(s.stats.income)}</strong></div><div class="log-row"><span>Total pengeluaran</span><strong>${money(s.stats.expense)}</strong></div><div class="log-row"><span>Termasuk biaya operasional</span><strong>${money(s.stats.operating)}</strong></div><div class="log-row"><span>Hasil bersih</span><strong>${money(s.stats.income-s.stats.expense)}</strong></div><p class="mini-sub sectiongap">Setoran, penarikan, pencairan pokok pinjaman, dan pembayaran pokok adalah perpindahan saldo; tidak dihitung sebagai laba.</p></div><h3 class="sectiongap">Buku kas</h3>${s.ledger.slice(0,20).map(l=>`<div class="log-row"><span>${esc(l.label)}</span><strong style="color:${l.amount<0?'#a65f47':'#367a55'}">${l.amount>0?'+':''}${fmt(l.amount)}</strong></div>`).join('')}`;}
 function harborPanel(){const s=state,cap=HH.capacities(s);let html=`<p class="panel-intro">Siapkan muatan di gudang pelabuhan, pilih tujuan, lalu kirim kapalmu. Hasil diterima satu kali setelah kapal kembali.</p>`;if(!HH.has(s,'harbor'))return html+`<div class="empty">⚓<h3 class="sectiongap">Cakrawala sedang menunggu.</h3>${esc(HH.needReason(s,HH.B.harbor.need)||'Teknologi siap. Bangun pelabuhan di tepi laut kanan.')}<div class="material sectiongap">${mat(HH.B.harbor.mat)}<br>${money(HH.B.harbor.cost)}</div><div class="actions">${button('Lihat bangunan →','view',{view:'Bangunan'})}</div></div>`;
  html+=`<div class="statgrid"><div class="statbox"><small>Armada / tambatan</small><strong>${s.ships.length}/${cap.berths}</strong></div><div class="statbox"><small>Reputasi</small><strong>✦ ${fmt(s.reputation)}</strong></div></div><div class="card"><h3>📦 Muatan pelabuhan</h3><small>${cap.portUsed}/${cap.port} ruang digunakan</small><label for="transferItem">Barang</label><select id="transferItem">${Object.keys(HH.ITEMS).filter(k=>k!=='vessel').map(k=>`<option value="${k}" ${transferItem===k?'selected':''}>${HH.ITEMS[k][0]} · gudang ${HH.count(s,k)} / pelabuhan ${s.portStock[k]||0}</option>`).join('')}</select><label for="transferQty">Jumlah</label><input id="transferQty" type="number" value="5" min="1" max="100000"><div class="grid2 sectiongap">${button('Ke pelabuhan →','transferUI',{to:1})}${button('← Ke gudang','transferUI',{to:0})}</div></div><h3 class="sectiongap">Armada kapal</h3>`;
  if(!s.ships.length)html+=`<div class="empty">Bangun Galangan Kapal di tepi laut, lalu pilih resep kapal di menu Produksi.</div>`;for(const ship of s.ships){const t=ship.trip;html+=`<div class="card ship-card sectiongap"><div class="row"><span class="bigicon">⛵</span><div><strong>${esc(ship.name)}</strong><div class="mini-sub">Lv.${ship.level} · ${HH.shipCap(ship)} muatan · kondisi ${ship.condition}%</div></div></div>${t?`<p class="mini-sub sectiongap">${HH.ROUTES[t.route].name} · ${t.mode==='expedition'?'Ekspedisi':'Perdagangan'} · hadiah ${money(t.reward)}</p>${progress(t.time-t.left,t.time)}<small>${t.status==='ready'?'Kapal telah kembali':time(t.left)+' menuju kembali'}</small><div class="actions">${button('Terima hasil','claim',{id:ship.id},'small',t.status!=='ready')}</div>`:`<div class="actions">${button('Siapkan pelayaran','chooseShip',{id:ship.id},'small')}${button('Perbaiki · 25','service',{id:ship.id,up:0},'ghost small')}${button(`Upgrade · ${150*ship.level}`,'service',{id:ship.id,up:1},'ghost small',ship.level>=5)}</div><small>Perbaikan: 2 papan. Upgrade: 8 papan, 4 kain, 2 komponen.</small>`}</div>`;}
  const ship=s.ships.find(x=>x.id===shipUI.id)||s.ships.find(x=>!x.trip);if(ship&&!ship.trip){shipUI.id=ship.id;const cons=s.contracts.filter(c=>c.type==='export'&&c.status==='active');if(shipUI.contractId&&!cons.some(c=>c.id===shipUI.contractId))shipUI.contractId=null;let q;try{q=HH.quote(s,ship,shipUI.route,shipUI.item,shipUI.qty,shipUI.mode,shipUI.contractId);}catch{}html+=`<div class="card sectiongap"><h3>Pelayaran ${esc(ship.name)}</h3><label for="voyageRoute">Tujuan</label><select id="voyageRoute">${Object.entries(HH.ROUTES).map(([k,r])=>`<option value="${k}" ${k===shipUI.route?'selected':''}>${r.name}${HH.routeReason(s,k)?' · '+HH.routeReason(s,k):''}</option>`).join('')}</select><label for="voyageMode">Jenis</label><select id="voyageMode"><option value="trade" ${shipUI.mode==='trade'?'selected':''}>Perdagangan</option><option value="expedition" ${shipUI.mode==='expedition'?'selected':''}>Ekspedisi · 2 peti + 2 roti</option></select>${shipUI.mode==='trade'?`<label for="voyageContract">Kontrak</label><select id="voyageContract"><option value="">Perdagangan bebas</option>${cons.map(c=>`<option value="${c.id}" ${c.id===shipUI.contractId?'selected':''}>${c.qty} ${HH.ITEMS[c.item][0]} · ${c.reward} koin</option>`).join('')}</select>${!shipUI.contractId?`<label for="voyageItem">Barang dagangan</label><select id="voyageItem">${Object.keys(HH.ITEMS).filter(k=>k!=='vessel').map(k=>`<option value="${k}" ${shipUI.item===k?'selected':''}>${HH.ITEMS[k][0]} · ${s.portStock[k]||0} tersedia</option>`).join('')}</select><label for="voyageQty">Jumlah muatan</label><input id="voyageQty" type="number" min="1" max="${HH.shipCap(ship)}" value="${shipUI.qty}">`:''}`:''}${q?`<div class="notice sectiongap"><strong>Rincian sebelum berangkat</strong><br>${Object.entries(q.cargo).map(([k,v])=>`${v} ${HH.ITEMS[k][0]}`).join(' + ')}<br>Biaya ${money(q.cost)} · ${time(q.time)}<br>Hadiah ${money(q.reward)} + ${q.rep} reputasi${HH.sum(q.goods)?'<br>Impor: 2 kristal laut + 4 bijih besi':''}</div>`:''}<div class="actions">${button('Berangkat →','sendUI',{},'',!!HH.routeReason(s,shipUI.route)||!q)}</div></div>`;}
  html+='<h3 class="sectiongap">Jaringan kepulauan</h3>';for(const[k,r]of Object.entries(HH.ROUTES))html+=`<div class="card route-card sectiongap"><div class="row between"><strong>${r.icon} ${r.name}</strong><span class="pill">${HH.routeReason(s,k)?'TERKUNCI':'TERBUKA'}</span></div><p class="mini-sub sectiongap">Dicari: ${r.goods.map(g=>HH.ITEMS[g][0]).join(', ')} · harga ×${r.mult}. Barang lain ×1,25.<br>Biaya ${money(r.cost)} · ${time(r.time)} dasar.<br>${esc(HH.routeReason(s,k))}</p></div>`;
  html+=`<h3 class="sectiongap">Kontrak ekspor</h3>${!HH.has(s,'office')?'<div class="notice">Bangun Kantor Perdagangan untuk memperoleh kontrak besar.</div>':''}`;for(const c of s.contracts.filter(c=>c.type==='export'))html+=`<div class="contract"><strong>${c.qty} ${HH.ITEMS[c.item][0]} → ${money(c.reward)}</strong><p class="mini-sub" style="margin:7px 0 0">+${c.rep} reputasi · ${c.status==='sailing'?'Dalam pelayaran':c.status==='completed'?'Selesai':time(c.left)}</p></div>`;return html;
 }
 function accountPanel(){const s=state;return `<p class="panel-intro">${esc(user.email)}<br>Satu akun menyimpan satu dunia utama.</p><div class="card"><h3>Profil pemain</h3><form id="profileForm"><label for="newName">Nama pemain</label><input id="newName" name="name" minlength="2" maxlength="32" value="${esc(profile.player_name)}" required><button class="full secondary sectiongap">Simpan nama</button></form></div><div class="card sectiongap"><h3>Ganti password</h3><form id="passwordForm"><label for="newPassword">Password baru</label><input id="newPassword" type="password" name="password" minlength="8" maxlength="128" autocomplete="new-password" required><label for="newPasswordConfirm">Konfirmasi</label><input id="newPasswordConfirm" type="password" name="confirm" minlength="8" maxlength="128" autocomplete="new-password" required><button class="full secondary sectiongap">Ganti password</button></form></div><div class="card sectiongap"><div class="row between"><h3>Cloud save</h3><span class="pill">REVISI ${revision}</span></div><p class="mini-sub" id="accountSaveStatus">${{saved:'Tersimpan',saving:'Sedang menyimpan',dirty:'Perubahan belum tersinkron',offline:'Koneksi terputus',conflict:'Konflik progres'}[connection]}</p><div class="stack">${button('☁ Simpan sekarang','save',{},'secondary')}${button('Ekspor cadangan JSON','export',{},'ghost')}<label class="ghost" for="importFile">Impor cadangan JSON</label><input id="importFile" type="file" accept=".json,application/json"></div><p class="online-note">Sinkronisasi setiap 20 detik saat ada perubahan. Progres saat ditutup dibatasi 8 jam. Impor mengganti progres akun ini setelah konfirmasi.</p></div><div class="card sectiongap"><div class="row between"><strong>Suara interaksi</strong><input id="soundToggle" class="sound-toggle" type="checkbox" ${s.settings.sound?'checked':''} aria-label="Aktifkan suara"></div></div><h3 class="sectiongap">Pencapaian</h3>${Object.entries(HH.ACH).map(([k,[n,v]])=>`<div class="achievement ${s.achievements.includes(k)?'':'off'}"><span>${s.achievements.includes(k)?'🏅':'○'}</span><span class="grow">${n}</span><small>+${v} koin</small></div>`).join('')}<div class="stack sectiongap">${button('Logout','logout',{},'secondary')}${button('Reset progres','reset',{},'danger')}</div>`;}
 function bindForms(){
   const change=(id,fn)=>{if($(id))$(id).onchange=fn;};change('marketItem',e=>{marketItem=e.target.value;renderPanel();});change('productionBuilding',e=>{selectedB=Number(e.target.value);renderPanel();});change('buildingFilter',e=>{buildingFilter=e.target.value;renderPanel();});change('autoCrop',e=>{state.autoCrop=e.target.value;mark(true);});change('transferItem',e=>{transferItem=e.target.value;});change('voyageRoute',e=>{shipUI.route=e.target.value;renderPanel();});change('voyageMode',e=>{shipUI.mode=e.target.value;shipUI.contractId=null;renderPanel();});change('voyageContract',e=>{shipUI.contractId=e.target.value?Number(e.target.value):null;renderPanel();});change('voyageItem',e=>{shipUI.item=e.target.value;renderPanel();});change('voyageQty',e=>{shipUI.qty=Number(e.target.value);renderPanel();});change('soundToggle',e=>{state.settings.sound=e.target.checked;mark(true);chime();});change('importFile',async e=>{const file=e.target.files[0];if(!file)return;try{if(file.size>2e6)throw Error('File maksimum 2 MB.');const next=HH.parseSave(await file.text());if(await ask('Ganti progres dengan cadangan?',`Progres akun ${user.email} akan diganti. Ekspor progres saat ini dahulu jika ingin menyimpannya. File hanya digunakan sebagai data game, tanpa mengambil identitas atau revisi server.`, 'Impor','IMPOR'))await replaceProgress(next);}catch(err){toast(humanError(err),true);}finally{if(e.target.isConnected)e.target.value='';}});
   if($('profileForm'))$('profileForm').onsubmit=async e=>{e.preventDefault();const name=e.currentTarget.elements.name.value.trim(),btn=e.currentTarget.querySelector('button'),gen=epoch;if(name.length<2){toast('Nama minimal 2 karakter.',true);return;}btn.disabled=true;try{const res=await client.from('profiles').update({player_name:name}).eq('id',user.id).select('player_name').single();if(gen!==epoch)return;if(res.error)throw res.error;profile.player_name=res.data.player_name;$('islandName').textContent='Pulau '+profile.player_name;toast('Nama pemain diperbarui.');}catch(err){if(gen===epoch)toast(humanError(err),true);}finally{if(btn.isConnected)btn.disabled=false;}};
   if($('passwordForm'))$('passwordForm').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,pass=f.elements.password.value,confirm=f.elements.confirm.value,gen=epoch;if(pass!==confirm){toast('Konfirmasi password tidak sama.',true);return;}const btn=f.querySelector('button');btn.disabled=true;try{const {error}=await client.auth.updateUser({password:pass});if(gen!==epoch)return;if(error)throw error;f.reset();toast('Password berhasil diganti.');}catch(err){if(gen===epoch)toast(humanError(err),true);}finally{if(btn.isConnected)btn.disabled=false;}};
 }
 async function replaceProgress(next){await syncNow();if(conflict||pending||saving)throw Error('Selesaikan sinkronisasi atau konflik dahulu sebelum mengganti progres.');if(!state||!user)return;state=next;state.lastSim=Math.floor(now()/1000)*1000;selectedB=selectedPlot=null;mark(true);renderHud();renderPanel();resetCamera();await syncNow();toast('Progres diganti.');}
 function perform(name,args={}){if(!state||conflict||loading)return;try{const before=state.achievements.length;HH.action(state,name,args);mark(true);renderHud();renderPanel();chime();if(state.achievements.length>before)toast('🏅 '+HH.ACH[state.achievements.at(-1)][0]);return true;}catch(e){toast(e.message,true);return false;}}
 function plotClick(id){const p=state.plots.find(x=>x.id===Number(id));if(!p)return;selectedPlot=p.id;const kind=!p.crop?'plant':!p.watered?'water':p.progress>=HH.duration(state,p)?'harvest':null;if(kind)perform('plot',{id:p.id,kind,crop:selectedCrop});else{setView('Kebun');toast(`Petak #${p.id}: ${time(HH.duration(state,p)-p.progress)} menuju panen.`);}}
 document.addEventListener('click',async e=>{const nav=e.target.closest('[data-view]');if(nav){setView(nav.dataset.view);return;}const el=e.target.closest('[data-action]');if(!el||el.disabled||!state)return;const d=el.dataset,act=d.action;try{
   if(act==='view')return setView(d.view);if(act==='selectCrop'){selectedCrop=d.crop;renderPanel();return;}if(act==='buildingTab'){buildingTab=d.tab;setView('Bangunan');return;}if(act==='selectB'){selectedB=Number(d.id);setView('Produksi');centerBuilding(selectedB);return;}if(act==='place'){setPlacement(d.type);return;}if(act==='moveB'){setPlacement('move',Number(d.id));return;}if(act==='plotClick'){plotClick(d.id);return;}
   if(act==='farmAll'){let n=0;for(const p of state.plots){try{HH.action(state,'plot',{id:p.id,kind:d.kind,crop:selectedCrop});n++;}catch{}}if(n){mark(true);renderHud();renderPanel();chime();toast(`${n} petak berhasil ${d.kind==='plant'?'ditanami':d.kind==='water'?'disiram':'dipanen'}.`);}else toast('Belum ada petak yang dapat diproses. Periksa bibit, air, koin, dan ruang gudang.',true);return;}
   if(act==='demolish'){const b=state.buildings.find(b=>b.id===Number(d.id));if(!b)return;if(await ask('Bongkar '+HH.B[b.type].name+'?',`Dapat kembali ${Math.floor(HH.B[b.type].cost*.25)} koin dan 50% material dasar. Bangunan harus kosong; kapasitas tersisa harus cukup. Utang bank tetap tercatat.`,'Bongkar'))perform('demolish',{id:Number(d.id)});return;}
   if(act==='buySupply')return perform('buy',{item:marketItem,qty:Number($('buyQty').value)});if(act==='bank')return perform('bank',{kind:d.kind,qty:Number($('bankAmount').value)});if(act==='transferUI')return perform('transfer',{item:transferItem,qty:Number($('transferQty').value),toPort:d.to==='1'});if(act==='chooseShip'){shipUI.id=Number(d.id);renderPanel();return;}if(act==='sendUI')return perform('send',{...shipUI});if(act==='save'){await syncNow();return;}if(act==='export'){exportState();return;}if(act==='logout'){await logout();return;}if(act==='reset'){if(await ask('Mulai dari kebun kecil lagi?',`Semua progres permainan akun ${user.email} akan diganti dengan permainan baru. Akun dan password tetap ada. Ekspor save dahulu bila ingin menyimpan progres lama.`,'Reset progres','RESET'))await replaceProgress(HH.fresh(now()));return;}
   const args={...d};delete args.action;for(const k of ['id','qty','index','delta'])if(args[k]!==undefined)args[k]=Number(args[k]);if(act==='repeat')args.recipe=d.recipe||null;if(act==='service')args.up=d.up==='1';perform(act,args);
  }catch(err){toast(humanError(err),true);}});
 $('saveNow').onclick=syncNow;
 document.addEventListener('visibilitychange',()=>{if(document.hidden){checkpoint();syncNow();}else if(state&&!conflict){const result=HH.advance(state,now());if(result.seconds){mark();renderHud();renderPanel();}syncNow();}});
 window.addEventListener('pagehide',checkpoint);window.addEventListener('beforeunload',e=>{checkpoint();if(state&&(dirty||pending)){e.preventDefault();e.returnValue='';}});window.addEventListener('offline',()=>{if(state){setStatus('offline');checkpoint();}});window.addEventListener('online',()=>{if(state)syncNow();});
  const canvas=$('world'),ctx=canvas.getContext('2d'),CELL=48,camera={x:11,y:8.3,z:1},pointer={x:4,y:8},mapSize={w:0,h:0},pointers=new Map();let placementMode=null,drag=null,pinch=null,lastFrame=0;
 function resizeMap(){const r=canvas.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2);if(r.width<1||r.height<1)return;if(canvas.width!==Math.round(r.width*dpr)||canvas.height!==Math.round(r.height*dpr)){canvas.width=Math.round(r.width*dpr);canvas.height=Math.round(r.height*dpr);}mapSize.w=r.width;mapSize.h=r.height;ctx.setTransform(dpr,0,0,dpr,0,0);}
 function resetCamera(){resizeMap();camera.x=mapSize.w<500?6.5:12;camera.y=mapSize.w<500?7.5:8.7;camera.z=mapSize.w<500?Math.min(1,Math.max(.6,mapSize.w/(12*CELL))):Math.min(1.3,Math.max(.45,Math.min(mapSize.w/(23*CELL),mapSize.h/(16*CELL))));updateZoom();}
 function updateZoom(){$('zoomLabel').textContent=Math.round(camera.z*100)+'%';}
 function zoom(mult){camera.z=Math.min(2.5,Math.max(.35,camera.z*mult));updateZoom();}
 function centerBuilding(id){const b=state.buildings.find(x=>x.id===id);if(b){camera.x=b.x+HH.B[b.type].w/2;camera.y=b.y+HH.B[b.type].h/2;camera.z=Math.max(camera.z,.9);updateZoom();}}
 function worldPoint(e){const r=canvas.getBoundingClientRect();return{x:(e.clientX-r.left-mapSize.w/2)/(CELL*camera.z)+camera.x,y:(e.clientY-r.top-mapSize.h/2)/(CELL*camera.z)+camera.y};}
 function setPlacement(type,id=null){placementMode={type,id};$('placementHint').classList.remove('hidden');const label=type==='land'?'Beli lahan · 15 koin / petak':type==='road'?'Jalan · 3 koin. Klik jalan lama untuk membongkar.':type==='plot'?'Tambah petak kebun · 30 koin':type==='move'?`Pindahkan ${HH.B[state.buildings.find(b=>b.id===id).type].name}`:`Bangun ${HH.B[type].name} · ${HH.B[type].cost} koin`;$('placementHint').innerHTML=`<span>${esc(label)}<br><small>Hijau: lokasi tersedia · merah: terhalang</small></span><button id="cancelPlacement" class="ghost small">Selesai</button>`;$('cancelPlacement').onclick=cancelPlacement;canvas.style.cursor='crosshair';canvas.focus({preventScroll:true});}
 function cancelPlacement(){placementMode=null;$('placementHint').classList.add('hidden');canvas.style.cursor='grab';}
 function placeAt(x,y){if(!placementMode)return;const{type,id}=placementMode;if(type==='land')perform('land',{x,y});else if(type==='road')perform('road',{x,y});else if(type==='plot')perform('newplot',{x,y});else if(type==='move'){if(perform('move',{id,x,y}))cancelPlacement();}else{if(perform('build',{type,x,y})){toast(HH.B[type].name+' mulai dibangun.');cancelPlacement();}}}
 function mapClick(x,y){if(!state||conflict)return;if(placementMode){placeAt(x,y);return;}const b=state.buildings.find(b=>x>=b.x&&x<b.x+HH.B[b.type].w&&y>=b.y&&y<b.y+HH.B[b.type].h);if(b){selectedB=b.id;setView('Produksi');return;}const p=state.plots.find(p=>!p.greenhouse&&p.x===x&&p.y===y);if(p){plotClick(p.id);return;}}
 canvas.addEventListener('pointerdown',e=>{canvas.setPointerCapture(e.pointerId);pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===2){const[a,b]=[...pointers.values()];pinch={distance:Math.hypot(a.x-b.x,a.y-b.y),z:camera.z};if(drag)drag.moved=true;}else drag={x:e.clientX,y:e.clientY,camX:camera.x,camY:camera.y,moved:false};pointer.x=Math.floor(worldPoint(e).x);pointer.y=Math.floor(worldPoint(e).y);});
 canvas.addEventListener('pointermove',e=>{const p=worldPoint(e);pointer.x=Math.floor(p.x);pointer.y=Math.floor(p.y);if(!pointers.has(e.pointerId))return;pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(pointers.size===2&&pinch){const[a,b]=[...pointers.values()];camera.z=Math.min(2.5,Math.max(.35,pinch.z*Math.hypot(a.x-b.x,a.y-b.y)/Math.max(1,pinch.distance)));updateZoom();return;}if(drag&&!pinch){const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.abs(dx)+Math.abs(dy)>6)drag.moved=true;if(drag.moved){camera.x=Math.max(-3,Math.min(HH.W+3,drag.camX-dx/(CELL*camera.z)));camera.y=Math.max(-3,Math.min(HH.H+3,drag.camY-dy/(CELL*camera.z)));}}});
 canvas.addEventListener('pointerup',e=>{const point=worldPoint(e);if(drag&&!drag.moved&&!pinch)mapClick(Math.floor(point.x),Math.floor(point.y));pointers.delete(e.pointerId);if(!pointers.size){drag=null;pinch=null;}});canvas.addEventListener('pointercancel',e=>{pointers.delete(e.pointerId);drag=null;pinch=null;});
 canvas.addEventListener('wheel',e=>{e.preventDefault();zoom(e.deltaY<0?1.1:1/1.1);},{passive:false});canvas.addEventListener('keydown',e=>{if(e.key==='Escape'){cancelPlacement();return;}const keys={ArrowLeft:[-1,0],ArrowRight:[1,0],ArrowUp:[0,-1],ArrowDown:[0,1]};if(keys[e.key]){e.preventDefault();const[dx,dy]=keys[e.key];if(placementMode){pointer.x+=dx;pointer.y+=dy;}else{camera.x+=dx;camera.y+=dy;}}else if(e.key==='Enter'&&placementMode){e.preventDefault();placeAt(pointer.x,pointer.y);}else if(e.key==='+'||e.key==='=')zoom(1.2);else if(e.key==='-')zoom(1/1.2);});
 $('zoomIn').onclick=()=>zoom(1.2);$('zoomOut').onclick=()=>zoom(1/1.2);$('zoomReset').onclick=resetCamera;new ResizeObserver(()=>{const small=mapSize.w<500;resizeMap();if(small!==(mapSize.w<500))resetCamera();}).observe(canvas);
 const noise=(x,y)=>{const n=Math.sin(x*127.1+y*311.7)*43758.5453;return n-Math.floor(n);};
 function rect(x,y,w,h,color,r=0){ctx.fillStyle=color;if(r){ctx.beginPath();ctx.roundRect(x,y,w,h,r);ctx.fill();}else ctx.fillRect(x,y,w,h);}
 function circle(x,y,r,color){ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fillStyle=color;ctx.fill();}
 function textMap(t,x,y,size=12,color='#354c36',align='center'){ctx.font=`600 ${size}px "Segoe UI",sans-serif`;ctx.fillStyle=color;ctx.textAlign=align;ctx.textBaseline='middle';ctx.fillText(t,x,y);}
 function tree(x,y,scale=1){ctx.save();ctx.translate(x,y);ctx.scale(scale,scale);ctx.fillStyle='#4c723322';ctx.beginPath();ctx.ellipse(8,10,17,9,0,0,7);ctx.fill();rect(-3,-6,6,20,'#886548',2);circle(0,-14,18,'#5f914e');circle(-10,-20,12,'#73a256');circle(10,-21,13,'#75a759');circle(0,-29,12,'#83b35e');ctx.restore();}
 function plantDrawing(p,t){const x=p.x*CELL,y=p.y*CELL;rect(x+2,y+2,44,44,'#a27850',6);rect(x+4,y+4,40,38,p.watered?'#79583e':'#947149',5);for(let i=0;i<3;i++)rect(x+8,y+11+i*10,32,2,'#694d343e',1);if(!p.crop){textMap('+',x+24,y+23,20,'#c5ae79');return;}const ratio=p.progress/HH.duration(state,p),ripe=ratio>=1;for(let i=0;i<6;i++){const cx=x+12+i%3*12,cy=y+16+Math.floor(i/3)*17,height=4+ratio*9;ctx.strokeStyle=ripe&&p.crop==='wheat'?'#ead06a':'#739942';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy+5);ctx.lineTo(cx+Math.sin(t/800+i)*.5,cy-height);ctx.stroke();ctx.fillStyle='#8eaf54';ctx.beginPath();ctx.ellipse(cx-3,cy-height/2,5,2,-.5,0,7);ctx.ellipse(cx+3,cy-height/2-3,5,2,.5,0,7);ctx.fill();if(ratio>.45){const col={wheat:'#f2d271',corn:'#edca4e',tomato:'#da6849',cane:'#b3cd69',cotton:'#f9f5e5',berry:'#d85250'}[p.crop];circle(cx,cy-height,2+ratio*2,col);}}
   if(!p.watered)textMap('💧',x+37,y+10,10);if(ripe){circle(x+39,y+6,7,'#fffcdb');textMap('✓',x+39,y+6,10,'#527344');}if(selectedPlot===p.id){ctx.strokeStyle='#fffbea';ctx.lineWidth=2;ctx.strokeRect(x+1,y+1,46,46);}}
 function buildingDrawing(b,t){const d=HH.B[b.type],x=b.x*CELL,y=b.y*CELL,w=d.w*CELL,h=d.h*CELL;ctx.save();ctx.translate(x,y);if(b.left){rect(7,12,w-9,h-15,'#a2895630',8);ctx.setLineDash([5,4]);ctx.strokeStyle='#f4e8b9';ctx.lineWidth=3;ctx.strokeRect(6,6,w-12,h-12);ctx.setLineDash([]);for(let i=0;i<d.w;i++){rect(10+i*40,12,5,h-20,'#b78e5b',1);}textMap('🔨',w/2,h/2-5,22);textMap(time(b.left),w/2,h/2+18,11,'#fbf4d8');ctx.restore();return;}
  rect(8,14,w-9,h-12,'#48653525',8);rect(4,7,w-8,h-9,'#d5d5ad',7);
  if(b.type==='well'){circle(w/2,h/2+4,16,'#e4d3a5');circle(w/2,h/2+4,11,'#538b96');rect(7,7,4,28,'#947044');rect(w-11,7,4,28,'#947044');rect(5,5,w-10,7,'#b56f50',3);}
  else if(b.type==='greenhouse'){rect(9,9,w-18,h-20,'#73ac92',5);rect(12,12,w-24,h-26,'#b1dac0',3);for(let i=0;i<3;i++){rect(15+i*22,20,15,h-45,'#63934f',4);rect(17+i*22,22,11,7,'#99bf6b',3);}ctx.strokeStyle='#e8eee0';ctx.lineWidth=3;ctx.strokeRect(12,12,w-24,h-26);ctx.beginPath();ctx.moveTo(w/2,12);ctx.lineTo(w/2,h-14);ctx.moveTo(12,h/2);ctx.lineTo(w-12,h/2);ctx.stroke();}
  else if(b.type==='silo'||b.type==='waterTower'){const col=b.type==='silo'?'#dcc797':'#8dbbc1';rect(17,15,w-34,h-25,col,8);ctx.fillStyle=b.type==='silo'?'#b16f52':'#6197a5';ctx.beginPath();ctx.ellipse(w/2,18,(w-34)/2,12,0,0,7);ctx.fill();rect(w/2-4,h-27,8,16,'#807b61',2);}
  else if(b.type==='harbor'||b.type==='shipyard'){rect(7,6,w-14,h-12,'#b89865',4);for(let i=10;i<h-7;i+=12)rect(7,i,w-14,2,'#8f744b45');rect(w-12,12,27,8,'#917348',2);rect(w-12,h-23,27,8,'#917348',2);rect(13,15,w-27,45,'#faf0cd',3);rect(8,10,w-17,29,b.type==='harbor'?'#598caa':'#da9062',4);textMap(d.icon,w/2,31,23);textMap(b.type==='harbor'?'HARVEST':'SHIPYARD',w/2,78,9,'#fff0c9');circle(14,h-16,4,'#706047');circle(w-18,h-16,4,'#706047');}
  else{const wall=b.level>=4?'#fff1cc':'#eee0b5';rect(12,24,w-24,h-36,wall,4);rect(9,18,w-18,Math.max(23,h*.4),d.color,4);ctx.fillStyle='#ffffff15';ctx.beginPath();ctx.moveTo(9,18);ctx.lineTo(w/2,9);ctx.lineTo(w-9,18);ctx.closePath();ctx.fill();rect(9,18,w-18,3,'#69452e20');rect(w/2-7,h-32,14,22,'#788573',3);rect(18,h-34,13,12,'#9db8a6',2);if(w>60)rect(w-31,h-34,13,12,'#9db8a6',2);textMap(d.icon,w/2,h*.35,22);if(['smelter','power','workshop','bakery'].includes(b.type)){rect(w-25,8,9,20,'#9d9787',2);if(b.job||b.fuel){for(let i=0;i<3;i++){const v=(t/700+i)%3;circle(w-20+v*3,5-v*10,4+v*1.2,`rgba(246,244,225,${.4-v*.1})`);}}}}
  if(b.type!=='well'){rect(w/2-18,h-14,36,13,'#f9f5dfe8',4);textMap(`Lv.${b.level}`,w/2,h-7.5,9,'#665e3f');}
  if(b.upgradeLeft)textMap('⬆',w-9,8,16,'#fff4bf');if(b.job){const r=HH.R[b.job.recipe];rect(12,h-3,w-24,4,'#ffffffb0',2);rect(12,h-3,(w-24)*(1-b.job.left/r.time),4,'#71aa65',2);}if(HH.sum(b.out)){circle(w-7,7,10,'#fff4c4');textMap('📦',w-7,7,13);}if(b.animals){textMap(b.type==='coop'?'🐔':'🐄',w-20,h-29,18);}
  if(selectedB===b.id){ctx.strokeStyle='#fcf7db';ctx.lineWidth=2;ctx.setLineDash([5,3]);ctx.strokeRect(1,1,w-2,h-2);ctx.setLineDash([]);}ctx.restore();
 }
 function drawMap(t){requestAnimationFrame(drawMap);if(t-lastFrame<33||!state||$('app').classList.contains('hidden'))return;lastFrame=t;resizeMap();const w=mapSize.w,h=mapSize.h;ctx.clearRect(0,0,w,h);rect(0,0,w,h,'#9fc27e');ctx.save();ctx.translate(w/2,h/2);ctx.scale(camera.z,camera.z);ctx.translate(-camera.x*CELL,-camera.y*CELL);
  rect(-CELL*8,-CELL*8,CELL*(HH.COAST+8),CELL*(HH.H+16),'#a8c987');rect(HH.COAST*CELL,-CELL*8,CELL*20,CELL*(HH.H+16),'#78b7bc');rect(HH.COAST*CELL,-CELL*8,20,CELL*(HH.H+16),'#94c8c6');rect(HH.COAST*CELL-10,-CELL*8,10,CELL*(HH.H+16),'#e5d69d');
  for(let y=0;y<HH.H;y++)for(let x=0;x<HH.W;x++){const n=noise(x,y),px=x*CELL,py=y*CELL;if(x<HH.COAST){const owned=state.land.includes(`${x},${y}`);rect(px,py,CELL,CELL,owned?(n>.5?'#b9d394':'#b4cf8e'):(n>.5?'#a2c47d':'#9dbf78'));if(owned){ctx.strokeStyle='#839c6130';ctx.lineWidth=.65;ctx.strokeRect(px+.4,py+.4,CELL-.8,CELL-.8);}for(let i=0;i<3;i++){circle(px+noise(x+i*2,y+5)*CELL,py+noise(y+i*3,x+7)*CELL,.8,owned?'#94b875':'#8fb269');}if(!owned&&n>.56&&x<HH.COAST-1)tree(px+25,py+30,.75+n*.25);if(!owned&&n<.07){rect(px+14,py+18,18,12,'#a0ac8a',5);rect(px+19,py+16,15,10,'#b6c1a2',4);}}else if(n>.2){ctx.strokeStyle='#ceede04d';ctx.lineWidth=2;ctx.beginPath();const wx=px+7+Math.sin(t/1600+y)*5,wy=py+17+n*20;ctx.moveTo(wx,wy);ctx.quadraticCurveTo(wx+9,wy+3,wx+20,wy);ctx.stroke();}}
  rect(HH.COAST*CELL-7,0,7,HH.H*CELL,'#e4d8a4');
  for(const key of state.roads){const[x,y]=key.split(',').map(Number);rect(x*CELL+1,y*CELL+1,CELL-2,CELL-2,'#d9c59c',5);for(let i=0;i<3;i++)rect(x*CELL+7+i*12,y*CELL+12+(i%2)*9,5,2,'#b59a7440',1);}
  state.plots.filter(p=>!p.greenhouse).forEach(p=>plantDrawing(p,t));state.buildings.slice().sort((a,b)=>a.y-b.y).forEach(b=>buildingDrawing(b,t));
  const workers=Math.min(18,HH.sum(state.workers.roles));for(let i=0;i<workers;i++){const x=(4+(i%5)*1.2+Math.sin(t/7000+i)*1.5)*CELL,y=(7+Math.floor(i/5)*1.5+Math.cos(t/6000+i)*1.2)*CELL;circle(x+2,y+6,5,'#60814330');rect(x-3,y-2,6,8,['#618b9e','#c88c59','#eee0a4'][i%3],2);circle(x,y-5,4,'#eccb95');rect(x-5,y-8,10,3,'#be994f',2);}
  state.ships.forEach((ship,i)=>{const sailing=ship.trip?.status==='sailing',p=sailing?1-ship.trip.left/ship.trip.time:0,x=(21.7+(sailing?Math.sin(p*Math.PI)*3:0))*CELL,y=(3+i*2.2+(sailing?p*3:0))*CELL+Math.sin(t/1300+i)*3;ctx.save();ctx.translate(x,y);ctx.rotate(sailing?.15:0);ctx.fillStyle='#467e872a';ctx.beginPath();ctx.ellipse(4,11,24,9,0,0,7);ctx.fill();ctx.fillStyle='#9a714b';ctx.beginPath();ctx.moveTo(-18,-4);ctx.lineTo(20,-4);ctx.lineTo(14,12);ctx.lineTo(-12,12);ctx.closePath();ctx.fill();rect(-2,-30,3,35,'#9c8160');ctx.fillStyle='#fff8d9';ctx.beginPath();ctx.moveTo(-1,-28);ctx.lineTo(-1,-3);ctx.lineTo(20,-3);ctx.closePath();ctx.fill();if(ship.trip?.status==='ready'){circle(18,-22,8,'#fff1b5');textMap('✓',18,-22,11,'#477653');}ctx.restore();});
  if(!state.ships.length){textMap('⛵',24*CELL,13*CELL+Math.sin(t/1400)*3,40);}
  textMap('LAUT HARVEST',23.5*CELL,HH.H*CELL-48,10,'#d6eeDF');
  if(placementMode){const x=pointer.x,y=pointer.y,type=placementMode.type,base=type==='move'?state.buildings.find(b=>b.id===placementMode.id)?.type:type,d=HH.B[base]||{w:1,h:1};let why=type==='land'?(x<0||y<0||x>=HH.COAST||y>=HH.H||state.land.includes(`${x},${y}`)?'invalid':''):type==='road'&&state.roads.includes(`${x},${y}`)?'':HH.placement(state,base,x,y,placementMode.id);rect(x*CELL,y*CELL,d.w*CELL,d.h*CELL,why?'#d46c556b':'#eaffc481',4);ctx.lineWidth=3;ctx.strokeStyle=why?'#b34336':'#4a8147';ctx.strokeRect(x*CELL+1,y*CELL+1,d.w*CELL-2,d.h*CELL-2);textMap(why?'×':type==='land'?'+':HH.B[base]?.icon||'+',(x+d.w/2)*CELL,(y+d.h/2)*CELL,23,why?'#9c3427':'#427440');}
  ctx.restore();
 }
 requestAnimationFrame(drawMap);

 init();
})();

</script>
</body></html>
